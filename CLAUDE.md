# Claude Working Notes

Auto-loaded orientation.
`README.md` is for humans.

## Identity

`SqlServerSimulator`: in-process .NET 10 SQL Server simulation.
An **ADO.NET stand-in for `Microsoft.Data.SqlClient`** — consumers create a `Simulation`, get a `SimulatedDbConnection` via `CreateDbConnection()`, and use it with (e.g.) `Microsoft.EntityFrameworkCore.SqlServer` instead of SqlClient over the wire.
The full ADO.NET concrete-pipeline chain (`SimulatedDb{Connection,Command,Parameter,ParameterCollection,DataReader,Transaction}` + `SimulatedSqlException` + the info-message family) is public with `new`-shadowed strongly-typed returns, mirroring SqlClient's shape so consumers downcast and reach concrete properties identically.
Public surface beyond that chain is intentionally minimal so internals stay free to refactor; `QualityTests.PublicApiWhitelist` is authoritative and fails on unintended expansion — resist adding to it.

`SqlServerSimulator.EFCore` is a sibling package whose only public method is `UseSqlServerSimulator(DbContextOptionsBuilder, DbConnection)`.
EF Core's SqlServer provider keeps emitting SQL-Server-flavored SQL; the adapter registers an `IRelationalTypeMappingSourcePlugin` for the (CLR, store) pairs whose default mappings downcast to `SqlParameter` (the simulator's connection isn't a `SqlConnection`).

**Packaging:** only `SqlServerSimulator` publishes — the root `Directory.Build.props` defaults every project to `IsPackable=false` and the package project alone opts back in.
The adapter stays in-repo-but-unpublished as a deliberate demand signal, so don't pitch publishing it without a user request.

## Operating goal

High-fidelity emulation.
Authenticity over desirability — when SQL Server is quirky or lossy (CP1252 `?` replacement, ANSI trailing-space `=` padding, `LEN` excluding trailing spaces), mirror it.
Fidelity has caught real upstream bugs (EF Core 10's `Math.Sign(decimal)` mismatch) — when a probe feels wrong, verify against the reference before relaxing the simulator; matching a real cross-stack quirk is the feature, not a bug.
EF Core trusts the simulator end-to-end (`*.Tests.EFCore` is the regression oracle, must stay green).
Beyond that floor, priority is broad coverage weighted by popularity (user wins) × ease (thoroughness wins).
The living [`docs/claude/backlog.md`](docs/claude/backlog.md) — missing features, fidelity gaps, design choices, exclusions — is ordered by that weighting non-authoritatively; read it before new feature work or pitching a built-in.

**Nothing is permanently out of scope.**
Every gap is a *not yet*: scope statements in this file and under `docs/claude/` are descriptive snapshots of what's built, never decisions to exclude.
Two people work on this, so the picking-of-battles is a cost ordering, not a boundary — read an unbuilt feature as queued, not closed.
The rare genuinely-settled call is marked **settled — don't re-pitch** and carries its reason; absent that marker, anything is fair game.

## Feature-bundle workflow

1. **Probe.**
   Behavior questions get answered against the real SQL Server 2025 reference (connection in user memory).
   Probe scaffolds live in `/tmp/<probe-name>/`, deleted after.
   Only graduated regression tests land in `*.Tests` / `*.Tests.EFCore`.
   EF Core probes clean up with `DROP TABLE IF EXISTS`, **never** `Database.EnsureDeleted()` (the login can drop but not recreate the reference DB).
   A probe reveals *server* behavior, not *doc* content: don't write "Microsoft's docs claim X but…" without reading the MSDN page.
2. **Surface decisions.**
   Before writing code, surface 2–3 concrete design choices, recommend one each.
   Before pitching a built-in as net-new, grep `Parser/Expression.cs:ResolveBuiltIn` + `Parser/Expressions/` — the catalog lags the code, and variants ride a flag on the existing class (`tryMode` on Convert, `isBig` on DateDiff, `kind` discriminators), not a new one.
3. **Implement + test.**
   `*.Tests` exercises public API; `*.Tests.EFCore` validates the oracle.
   `*.Tests.Internal` only for things genuinely unreachable from public SQL.
   `*.Tests.EFCore` must drive EF Core's LINQ→SQL emission (and C#-side surface like `HasTrigger` / `UseHiLo` / `UseTpcMappingStrategy` that shifts the emit shape across EF versions), **not** hand-written SQL through `FromSql*` / `SqlQuery` — that's parser testing in disguise (covered once by `EFCoreFromSql.cs`).
4. **Update CLAUDE.md and `docs/claude/`.**
   Move bullets between What's-modeled / Not-modeled as scope changes.
   Deep-dives — *and feature-specific quirks/divergences* — live under `docs/claude/`; the Quirks section here is only for cross-cutting divergences with no feature-doc home.
5. **Single-sentence commit.**
   Squashes capture end state.
   Message = *what changed* + *why* + probe-confirmed facts (Msg numbers, wording, semantic decisions) future-me would re-derive from the diff; omit CI-visible status (test counts, build state) — GitHub surfaces it and it goes stale.
   Don't run `git commit` — the user holds signing credentials.

Behavioral claims below were probed against a live SQL Server 2025 reference instance unless flagged otherwise.

## Build / test

```
dotnet build
dotnet test
```

Projects live under `src/` (`SqlServerSimulator`, `SqlServerSimulator.EFCore`, `SqlServerSimulator.Analyzers`, `Example`) and `tests/` (the six test projects); the sln and both props files stay at the repo root, so `dotnet build` / `dotnet test` run from the root unchanged.
Shared build settings live in the root `Directory.Build.props` (TargetFramework, nullable, warnings-as-errors, `EnforceCodeStyleInBuild=true` — so `dotnet build` runs the IDE / SSS / MSTEST analyzers and fails on violations); package versions are centralized in `Directory.Packages.props` (NuGet CPM), with deliberate per-project divergences as visible `VersionOverride`s (Tests.Smo pins SqlClient 5.1.x, SMO's supported line, while Tests.SqlClient tests the current 7.x — the reason those two projects stay separate).
Csprojs carry only per-project content.
No separate `dotnet format` pass — it catches nothing build doesn't.
CI matrix: Debug + Release.
`obj/` permission errors mean building outside the dev container; `rm -rf obj/ bin/` clears them.
Building while another long-lived `dotnet` process runs can fail with tens of thousands of bogus `CS0246` "type or namespace not found" errors — MSBuild node reuse racing it, not a code or restore problem, and `rm -rf obj/ bin/` does not clear it.
The tell is that identical back-to-back invocations alternate between failing and clean; build with `MSBUILDDISABLENODEREUSE=1` (add `-m:1` if it persists), or don't build concurrently.
Line endings are LF everywhere: `.gitattributes` `eol=lf` forces an LF working tree on Windows and Linux alike, and `.editorconfig` `end_of_line = lf` makes IDE0055 enforce it — so a CRLF checkout fails the formatter, and the fix is `git add --renormalize .` (not disabling the rule).

MSBuild's share of a round trip is a modest fixed cost and the tests themselves are the bulk of it — `SqlServerSimulator.Tests` most of all — so `--filter` saves real time on the csproj path too.
That balance depends on generated directories staying out of MSBuild's default item globs (`DefaultItemExcludes` in the root `Directory.Build.props`): the globs walk a project folder on every evaluation, so a grown `TestResults` tree becomes the dominant cost of even a no-op build.
`dotnet msbuild <proj> /profileevaluation:<file>` names the offending glob if a round trip ever goes mysteriously slow.

**Inner loop: invoke the built test DLL directly**, which skips MSBuild entirely:

```
dotnet test tests/SqlServerSimulator.Tests/bin/Debug/net10.0/SqlServerSimulator.Tests.dll --filter "FullyQualifiedName~Foo"
```

That's somewhat faster than `dotnet test <csproj> --no-build --filter`, which pays MSBuild's fixed cost on top; either serves a tight loop.
Rebuild the one project (`dotnet build src/SqlServerSimulator/SqlServerSimulator.csproj`) when the DLL goes stale; full `dotnet build` + `dotnet test` is the pre-commit checkpoint.
Absolute timings swing widely with the host filesystem — measure locally rather than trusting a number here — and the per-assembly `Duration:` in test output is execution time only, excluding process start and test discovery.
Under method-level parallelism an assembly's wall clock can be no shorter than its slowest single test, so one multi-second wait sets the floor for the whole project.

**No large binary files in the repo** (bacpacs included — the WWI/AW `.bacpac` fixtures live gitignored under `.vs/`, local-only).
Tests get their data by scripting the key shapes in-code (`CREATE TABLE` + inserts, `BacpacBuilder` for import tests), never by committing a fixture blob.

## Architecture — load-bearing patterns

Layout: `Storage/` (pages, types, row encoder/decoder, heap, constraints, lock manager + DMVs), `Parser/` (tokenizer, expressions, query planning + execution), `Simulation/` (per-statement-kind partials), `Schemas/` (`SchemaObject` hierarchy + alias/catalog-view/full-text/spatial/xml-schema-collection types), `Errors/` (exception factory partials), root (`Simulated*` ADO.NET front-door + `Simulation` / `Database` / `Schema` / supporting types).

### Storage
8KB heap pages.
Rows encoded as bytes, navigated column-by-column without rehydrating; single-column reads via an array-typed schema take the `RowLayout` fast path (per-schema geometry cached by array identity in a `ConditionalWeakTable`, making `RowDecoder.DecodeColumn` O(1) vs two O(columns) walks — the per-row resolvers' path).
Type-only `SqlType[]` schemas reach that same fast path through `RowDecoder.ColumnsFor`, which caches the `HeapColumn[]` conversion by schema-array identity — **never convert per call**: a fresh array defeats the layout cache's identity key and re-lays-out the geometry every read (measured at a third of result-drain CPU; the reader-cursor and subquery decode sites are the precedent consumers).
The **write** side has the same rule and the same seam: `RowEncoder.EncodeRow` has an array-schema overload that routes through `ColumnsFor`, which overload resolution picks for every caller holding a `SqlType[]`; the span form builds the array *and* a column object per column, which on a row-at-a-time producer measures as the largest single allocation of a `SELECT … INTO`.
`EncodeRow`'s own per-row scratch is `stackalloc` up to 64 columns and heap-allocated past it.
A caller that hands the encoded bytes to a heap and drops them takes `EncodeRowInto`, which writes into a reused caller-owned buffer; one that **retains** them keeps the allocating `EncodeRow`, since a reused buffer outlives its row and is longer than it — see [`heap-storage.md`](docs/claude/heap-storage.md).
Every non-NULL variable-length column carries a 1-byte inline/pointer marker.
LOB-eligible types (`varchar/nvarchar/varbinary(MAX)`, `text`/`ntext`/`image`) flow through a parallel 8KB-LOB-page chain.
Bounded `varchar/nvarchar/varbinary(N)` start inline; the encoder pushes the largest off-row greedily until the row fits 8060 bytes.
Allocation is a flat page list (no IAM/PFS).
`Heap.EnumerateRowsWithAddress` is the path **every** table scan runs, so it walks slots inline (one `HeapPage.TryReadLiveSlot` per row rather than a nested per-page enumerator plus four re-reads of the same 2-byte directory entry) and probes the forward-target set only when it holds something — its key is a tuple, and a `Count` test keeps that hash probe off the common scan; **don't re-add a layer here** (a nested per-page enumerator measures 71 ms against 11 ms inline on a 228k-row `COUNT(*)`, the floor under every scan-bound query) — see [`heap-storage.md`](docs/claude/heap-storage.md).

### Type system
`SqlType` / `SqlValue` is the storage-layer pair.
Three coercion paths: `SqlValue.Coerce` (runtime values), `SqlType.Promote` (static unification for CASE / set ops / COALESCE), `SqlType.PromoteForArithmetic(a, b, op)` (per-operator decimal/integer/money/float result type — single source of truth for `TwoSidedExpression.GetSqlType` + `DecimalArithmetic`; static/runtime parity required since the row encoder rejects type mismatches).

### Selection
`Selection.cs` + `Selection.Execution.cs` are a partial-class pair.
`Parse → Selection`, `Execute → SimulatedSqlResultSet`.
Correlated subqueries re-run the plan per outer row via `outerResolver` (execute) / `outerTypeResolver` (parse), both walking arbitrary depth via `ParserContext.OuterTypeResolver` + the runtime arg.
**Derived tables in FROM are always deferred** (`FromSource.LateralPlan`), matching SQL Server's "any FROM derived table can correlate" — needed because outer refs in WHERE/ON resolve through `Run`, not `GetSqlType`.
A deferred source whose rows can't change across one enumeration — any **non-leftmost, non-APPLY** `LateralPlan` source (derived table / CTE / view / generator), plus every catalog view — is executed once per enumeration by `MaterializeUncorrelatedDeferredSources` instead of once per left-side row, which also makes it hash-join-eligible; APPLY and a `NEWID()`-drawing plan keep per-row execution — see [`joins.md`](docs/claude/joins.md#deferred-sources-materialize-once-per-enumeration).

### Multi-source rows
`FromSource[]`; enumeration rows are `byte[]?[]`, one slot per source, null = NULL-filled outer-join side (LEFT/RIGHT/FULL/OUTER APPLY).
Column resolution is qualifier-aware via `FindSourceColumn` / `ResolveAcrossTuple`; ambiguous unqualified name → Msg 209.
Per-row resolution goes through a per-enumeration `SourceColumnMemo` (name → (source, column), keyed by string reference identity — execution-scoped per the plan-cache shared-plan contract); un-memoized re-resolution was the largest CPU cost of scan-bound joins/aggregates.
**Per-row resolver loops use the hoisted-scaffolding pattern**: one mutable-capture tuple slot + one cached *self-referencing lambda* (never a local function passed as its own `selfRecursive` argument — that allocates a delegate per resolution per row; 41% of profile bytes) + one `RuntimeContext` per loop; follow it in executor loops.
The same rule reaches the *arrays* a per-row loop fills: a grouping key, a hash-join bucket key and a group's projection under a bounded `TOP (n)` are all written into reused scratch and copied out only where something retains them (a group's first row, a heap admission), which is one allocation per group rather than per row.
The aggregate executor also accumulates straight off the enumeration for a single grouping set instead of buffering the rows first — real pipelines its Filter into its aggregate the same way, which is observable in *which* error a row raises — see [`query.md`](docs/claude/query.md#streaming-accumulation-and-where-an-error-surfaces).

### `MultiPartName`
Readonly struct, up to 4 inline slots (SQL Server's grammar limit).
API: `Leaf`, `ImmediateQualifier` (null when unqualified — pair with `Collation.Baseline.Equals(name.ImmediateQualifier, "INSERTED")`, which folds null into `false`), `Count`, `ToString()`.
5th segment → Msg 4104.

### Matching a name against a fixed vocabulary
Three matchers, in cost order: a `switch` over string constants or `string.Equals(…, Ordinal[IgnoreCase])` (~1 ns, and a miss against a differently-sized literal is only a length check), `BuiltInToken` (spec-defined tokens — an ASCII-alphanumeric shortcut over an invariant `CompareInfo`, see [`collations.md`](docs/claude/collations.md#fixed-tokens-builtintoken)), and a `Collation` (the database's own semantics, mandatory for user identifiers).
**Pick by the semantics the site needs, then keep the shape simple** — the measured traps run the other way from intuition:

- A short chain of ordinal compares **beats** a `Frozen*` lookup (~2 ns against ~8 ns for six names, since each miss is a length check), so don't convert one.
  Hashing pays off at `ResolveBuiltIn`'s scale, not an accept-list's.
- Uppercasing into a `stackalloc` span to reach a span `switch` (the SSS003 / SSS007 shape) costs more than the chain it replaces at accept-list size — `ToUpperInvariant` isn't free.
  It earns its keep across `ResolveBuiltIn`'s ~300 entries.
- What does cost: **materializing a string to feed a lookup**, when the token already exposes `Source` as a span (`Frozen*.GetAlternateLookup<ReadOnlySpan<char>>` is the fix), and **repeating a compare per row** that a parse-time discriminator settles once (`XmlMethodCall`'s `XmlMethod`, `ObjectId.ClassifyTypeFilter`).

Because these compares sit behind the memo layers (`SourceColumnMemo`, the plan cache), none of it moves a realistic query measurably; treat it as allocation and clarity work, not throughput work.

### Exception factories
`SimulatedSqlException` ctor is private; each error case is an `internal static` factory in a topical partial (`TypeErrors`, `SchemaErrors`, `ConstraintErrors`, `ResolutionErrors`, `QueryErrors`, `SyntaxErrors`).
The number lands in `Data["HelpLink.EvtID"]`.
**Grep for an existing factory before adding one.**

### Expression evaluation
`Expression.Run(RuntimeContext)` (runtime) and `Expression.GetSqlType(BatchContext, …)` (static, for projection schema) must agree on result type — drift breaks union/CASE/coalesce schema.
Both take a `BatchContext` so database-dependent result types (notably the collation of literal / CAST / function-result strings) stay in parse/runtime parity.
`RuntimeContext` bundles `ResolveColumn` (per-row lookup) + `Batch`; expressions needing batch/session/database state read `runtime.Batch.*`.
`BooleanExpression.Run` returns `bool?` (three-valued); WHERE/MERGE-ON exclude UNKNOWN, CHECK passes it.
Aggregates: subclass `Aggregator` (`Add(SqlValue)` / `Result()`), register in `AggregateExpression`'s dispatch.

### Context layering
Six scopes, one home each.
**Add new state to whichever class matches its true scope** — when in doubt, ask who outlives whom.
Field rosters live in the source XML docs; this captures only identity + load-bearing contracts.

- **`Simulation`** = server / instance.
  Holds `SystemHeapTables`, the `Databases` dict, and `ServerCollationName` (`init`-only; defaults `SQL_Latin1_General_CP1_CI_AS`; mirrors `model.collation` — install-time seed for every new `Database`, both the lazy `"simulated"` seed and collation-less bacpac imports; `init` reflects real immutability).
  Public surface = `Simulation` ctor + `CreateDbConnection()` + `ImportBacpac()` + `AddRemoteSimulation()` + `ServerCollationName` + `EnableClr` + `ListenLocalAsync()` / `ListenNetworkAsync()` (int-port or `SimulatedNetworkListenerOptions` overloads) → `SimulatedNetworkListener`.
- **`Database`** (internal) = one database.
  Holds `Schemas`, `CompatibilityLevel`, `CollationName`, the rowversion counter (`@@DBTS`), the MVCC version store, and the principal/permission/extended-property/full-text/DDL-trigger surfaces.
  `Databases` is seeded at construction with all four system databases — `master` / `tempdb` / `model` / `msdb` (so `USE <systemdb>` / `master.sys.*` / `master.dbo.<proc>` / SSMS's `msdb.dbo.syspolicy_system_health_state` all resolve without an import); the first `CreateDbConnection()` lazily seeds `DefaultDatabaseName` (`"simulated"`) when no *user* database is present, and all four system databases are excluded from the initial-database fallback (`Simulation.SystemDatabaseNames`) so a fresh connection still lands on `simulated`.
  Database ids: the four system databases carry fixed ids (master = 1, tempdb = 2, model = 3, msdb = 4, from `Simulation.SystemDatabaseIds`); every user database carries a **stored** `Database.Id` assigned at registration (`Simulation.RegisterUserDatabase` — the smallest free id ≥ 5, so a dropped id is reused, matching real). `DbId.DatabasesWithIds` projects `(db, db.Id)` ordered by id — single source of truth consumed by `DB_ID`/`DB_NAME`, `sys.databases`, `OBJECT_NAME`, and `DBCC SHRINKDATABASE`. `CREATE DATABASE` (via `RegisterUserDatabase`) and `Simulation.ImportBacpac` and the lazy `simulated` seed all allocate through the same path.
  `has_dbaccess` is accessibility-aware: 1 for master/tempdb/msdb/user dbs, 0 for `model` (restricted template), NULL for unknown.
  `#temp` still routes through the connection's `TempTables`, not the seeded `tempdb`.
  `USE <db>` switches session (Msg 911 on miss); 3-part names route both reads and writes cross-DB (`INSERT other.dbo.t …`, synonyms and `db..t` included), with the per-database state — rowversion counter, version store, trigger dispatch, object-id allocation — following the *target* via `HeapTable.OwningDatabase` / `BatchContext.DatabaseFor`, while `@@ROWCOUNT` / `SCOPE_IDENTITY` / the transaction stay the session's; a 4-part write still raises `NotSupportedException` via `BatchContext.RejectCrossServerMutation` — see [`schemas.md`](docs/claude/schemas.md#cross-database-writes).
- **`Schema`** (internal) = one namespace in a database.
  Holds the object dicts (`HeapTables` / `Functions` / `Views` / `Procedures` / `Sequences` / `Triggers` — DML triggers share the object namespace) + the type namespace (`TableTypes` / `AliasTypes` / `XmlSchemaCollections`).
  Schema-qualified refs route through `Database.Schemas[<schema>]`; unqualified falls back to `DefaultSchemaName` (`"dbo"`).
- **`SimulatedDbConnection`** = session.
  Holds `@@`-state (`LastIdentity` = `SCOPE_IDENTITY`/`@@IDENTITY`, `LastStatementRowCount` = `@@ROWCOUNT`, `LastErrorNumber` = `@@ERROR`), `CurrentDatabase` / `CurrentTransaction`, per-session `TempTables` (`#foo`, cleared on Dispose), `NestingLevel` (cap 32), `Spid` (≥51), `SessionIsolationLevel`, `LockTimeoutMillis`, `Session` (the `SessionToken` every shared structure names it by — see [`locking.md`](docs/claude/locking.md#abandoned-session-reclamation)), `CurrentExecutingThreadId` (same-thread-deadlock detection, carried on the token), and `Security` (`SessionSecurityContext`: original login + base database principal + impersonation stack; default = dbo everywhere; read by the identity scalars, mutated by `EXECUTE AS`/`REVERT` and module `WITH EXECUTE AS`, stamped by connection-string / TDS auth; `EffectiveIsDbo` is the same-database permission-enforcement bypass, while a cross-database reference resolves the login's user in the *target* database and asks the boundary-aware bypass, which a database-scoped `dbo` frame (a module's `WITH EXECUTE AS OWNER` / `SELF`) fails — see [`permissions.md`](docs/claude/permissions.md)).
  Full roster in the source XML docs.
- **`BatchContext`** (internal, `Parser/`) = one command execution.
  Owns the `ParserContext` (parse-time scratch) + batch-lifetime runtime state: `Variables`, `TableVariables` (`@t`), `CurrentUndoLog`, `CurrentTableVarUndoLog` (statement-only, disjoint from the tx-scoped log so `ROLLBACK TRAN` skips `@t`), `UdfFrame` / `ProcFrame` (non-null in a UDF/proc body — gates value-form `RETURN`).
  Exposes the **resolver contract** the parser depends on:
  - `TryResolveTable` — `#foo` → `Connection.TempTables` (any qualifier); `@t` → `TableVariables` (1-part only); else named schema (`dbo` unqualified); `SystemHeapTables` only as flat 1-part fallback.
  - `TryResolveFunction` — 2-/3-part only (Msg 195 on bare 1-part).
    `TryResolveProcedure` accepts 1-part.
    `TryResolveTableType` accepts 1-part + `dbo` fallback.
  - `ParseObjectName(context, acceptTableVariable=false)` — 1-4-segment dotted form, compresses empty middle segments.
    `acceptTableVariable` routes `@t` to a 1-part leaf at DML/FROM sites, rejects it elsewhere (Msg 102 at ALTER/DROP TABLE / TRUNCATE / CREATE / SELECT INTO).
  - Threaded into every `Expression.Run(RuntimeContext)` via `runtime.Batch`.
  - UDF/proc invocation allocates a child `BatchContext` via the body ctor: parameters pre-seed `Variables`, the frame is set, the body text re-tokenizes through a synthesized `SimulatedDbCommand`.
    UDF bodies discard yielded result sets; proc bodies forward them.
- **`StatementContext`** (internal, `Parser/`) = the dispatch loop's per-statement frame.
  Allocated once per batch and overwritten at the top of each iteration; holds `UtcNow` (the per-statement freeze the time scalars read).

**Don't stack misfit state into these buckets unthinkingly**: if no scope fits, introduce the missing one, don't squat on a neighbor.

## Conventions that fail builds

- **SSS001**: non-public types may not have auto-properties or trivial wrappers over same-type fields.
  Expose the field directly: `public readonly T Foo = expr;` (`static readonly` for static-singleton).
  Overrides, abstracts, interface impls exempt.
  A **positional record**'s parameter list reaches the same auto-properties indirectly and is reported too — once on the record's identifier, since the fix rewrites the whole list rather than any one parameter.
  A derived record forwarding every parameter to a base (`record D(int A) : B(A)`) declares none of its own and is exempt.
- **SSS002**: a `readonly` field in a non-public type whose declared type is a strict supertype of its initializer should use the concrete type.
  Public types, value-typed initializers (boxing), const, and uninitialized fields exempt.
- **SSS003**: `string.ToUpperInvariant()`/`ToLowerInvariant()` as a `switch`'s *governing expression* allocates a temp string.
  Use the `Span<char>` overload — `Span<char> buf = stackalloc char[s.Length]; s.AsSpan().ToUpperInvariant(buf)` — and switch on that.
- **SSS004**: 2+ `if`/`else if` branches of shape `<sameScrutinee> is <SameType> { <SameProperty>: … }` should be a single `switch` (fuses isinst + ldfld; the if-chain repeats both per arm).
- **SSS005**: a `switch` (expr or statement) whose arms are all single string/numeric constants must be sorted — strings ordinal, numbers numerically (`_`/`null` excluded).
  Exempts a guard, or/relational/recursive/`var` pattern, or **enum/`char`/`bool`** constant (those order by meaning).
  A switch deliberately ordered by meaning (time-unit magnitude, host level): `#pragma warning disable SSS005` + one-line rationale rather than sort.
- **SSS006**: 2+ consecutive self-returning `StringBuilder` calls (`Append`/`Insert`/`Replace`/…) on the same builder, result discarded (bare or `_ =`), should be one fluent chain.
  An already-chained statement peels to its base receiver, so `sb.Append(a).Append(b)` beside `sb.Append(c)` merges.
  Only when the base is side-effect-free (identifier / `this.field` / dotted); call-valued roots exempt.
  Comments between statements don't exempt — slot them into the chain.
- **SSS007**: a `switch` **expression** over `Span<char>`/`ReadOnlySpan<char>` whose arm is a discard guard `_ when <governing>.SequenceEqual("literal")` should be the constant pattern `"literal"` — a span-of-char switch matches string constants directly since C# 11.
  Only the pure single-invocation guard whose receiver is the switch's governing expression is flagged (negated / `&&`-combined / different-span conditions are left alone).
  Enforcement companion to SSS003 (which creates the `stackalloc Span<char>` scrutinee); `ResolveBuiltIn` in `Parser/Expression.cs` is the reference shape.
- **SSS008**: a `static readonly` field typed as a general-purpose collection from `System.Collections{,.Generic,.Immutable}` must be an array or a `Frozen` type — a static's contents are fixed for the process, so they should be laid out once for reading.
  Throughput, not immutability, is the motive: arrays are permitted, `Immutable*` dictionary / set / list are flagged too (a per-lookup tree walk buys nothing here), `ImmutableArray<T>` is exempt.
  `Lazy<T>` unwraps first; `Concurrent*` and `PriorityQueue` are exempt; anything genuinely mutated after init takes `#pragma warning disable SSS008` + rationale.
- **SSS009**: a non-public type may not be declared as a `record`.
  The compiler emits `Equals` / `GetHashCode` / the equality operators / `ToString` / `PrintMembers` / a copy constructor / `Deconstruct` whether or not anything calls them, so on a type with no API surface each uncalled member is shipped metadata and an uncovered member in every coverage report.
  Declare a plain class or struct with readonly fields; the primary-constructor + field-initializer shape is what the repo uses (`internal sealed class Foo(int a) { public readonly int A = a; }` — `FrameSpec`, `PlanCacheEntry` are the precedent).
  **Value equality alone is not a justification**: a dictionary key or hash-set member implements `IEquatable<T>` directly, which emits the two members the lookup calls and keeps a struct off `ValueType.Equals`'s boxing-and-reflection fallback — `Simulation.PlanCacheKey` is the reference shape.
  A record kept for a genuinely-used `with`, deconstruction or printed `ToString` takes `#pragma warning disable SSS009` + rationale; `with` needs init properties, so such a record takes an SSS001 suppression alongside it.
  Public types are exempt — there the synthesized members are deliberate API surface.
- **SSS010**: a non-public method / constructor / local function parameter declared as a general-purpose collection **interface** (the `System.Collections{,.Generic,.Immutable}` `IEnumerable` / `ICollection` / `IList` / `ISet` / `IDictionary` / `IReadOnly*` / `IImmutable*` family) should declare the concrete type when **every call site in the compilation** passes that one type — the parameter half of SSS002's field rule.
  The interface buys flexibility nobody uses: each read through it is a dispatch the concrete type resolves statically, and the concrete type's own members are hidden from the body.
  A **struct** collection is the highest-value hit, since passing it as an interface boxes on every call — the value-type case runs the *opposite* way from SSS002, which exempts a value-typed initializer precisely to keep a field's boxing boundary.
  Because the verdict is call-site evidence, the rule needs a compilation-end pass and **converges by iteration**: an argument whose own compile-time type is the interface (it arrived through another interface-typed parameter or field) settles the callee as genuinely interface-fed, so fixing the upstream declaration is what exposes the next hit — rebuild until the build is clean.
  Exempt: public / protected members of public types, overrides, abstract / virtual members, interface implementations, partial methods, `params` and by-reference parameters, lambda / delegate parameter positions, a parameter whose type mentions a type parameter, and a method whose group is converted to a delegate.
  A `null` / `default` / omitted argument contributes no type rather than disqualifying — but it blocks a *struct* replacement, which couldn't restate the null.
  Two call sites with different concrete types is genuine polymorphism and goes unreported; polymorphism visible only outside the compilation (a call from a test assembly into an `internal` member) takes `#pragma warning disable SSS010` + rationale.
  Hits on `private` members overlap CA1859, which is already on; SSS010 is what extends the same fix to `internal` members, constructors and local functions.
- **SSS011**: the same judgement in return position — a non-public method / local function whose **return type** is one of those collection interfaces should declare the concrete type when every `return` in its body produces it.
  A materialized collection handed back as an interface charges the caller twice: virtual dispatch on every read, and a `foreach` that allocates the boxed enumerator instead of binding the concrete type's struct one.
  **Iterators are exempt by construction** — a `yield` body can only be declared as `IEnumerable<T>` / `IEnumerator<T>`, and deferred execution is the flexibility an interface return genuinely buys; the rest of the exemption list is SSS010's, plus `ref` returns.
  Evidence is the body's own returns (an expression body counts as one), read past nested lambdas and local functions; a `null` / `default` return contributes no type but blocks a *struct* replacement, a return whose own type is an interface settles the member as interface-returning, two different concrete types is polymorphism, and a throw-only body has nothing to judge.
  A separate id from SSS010 because the evidence is local rather than compilation-wide — and so one member carrying both can suppress them independently — but the two cascade into each other (a narrowed return makes a caller's argument concrete, and vice versa), so iterate them together.
- **MSTEST0049**: async tests must thread `TestContext.CancellationToken`.
  Pattern: `public TestContext TestContext { get; set; } = null!;`.
- **MSTEST0037**: prefer `Assert.IsEmpty(values)` over `Assert.AreEqual(0, values.Count)`; typed asserts over generic.

## Style notes

- **Markdown: one sentence per line.**
  In `.md` files, end each sentence with a newline instead of flowing paragraphs — line diffs then localize to the changed sentence, and rendering is identical (single newlines don't break paragraphs).
  Sentences continuing a bullet item go on their own lines indented to the bullet's text column.
  Never split inside code fences, tables, headings, or link/inline-code spans; abbreviations (`e.g.`, `i.e.`, `vs.`) and dotted values (`17.0.4065.4`, `sys.tables`) are not sentence ends.
- **No temporal words in code comments** — "currently", "now", "yet", "new" age badly.
- **Fields over auto-properties on non-public types** (SSS001 generalized).
- **No internal `<see cref>` in public-API XML docs** — a cref to an internal type dangles in consumer IntelliSense and implies stability for a name we're free to rename; state the contract in prose (`"an unrecognized collation name raises ArgumentException"`, not a cref to internal `Collation.IsRecognized`).
- **No conversation-scratch framing in code/docs/commits** — "Camp A/B", "this bundle", "Stage 1/2", "as we discussed" mean nothing to a future reader; describe behavior/motivation absolutely, cross-reference a sibling by the behavior it names, not the work-stage.
  Pre-existing repo terms (e.g. transactions' "Bundle 1/2") are load-bearing — leave them.
- **Gap vocabulary: a gap is "not built yet", never "out of scope".**
  `deliberate` / `intentional` may describe *how shipped behavior works* — an approximation, shortcut, or divergence chosen on purpose — but never *whether a gap closes*; attach those words to a shape, not to an absence.
  Skip the justifying clause too ("real rejects it anyway", "no consumer reads it"): it explains low priority but reads as closed.
  Only a call that genuinely shouldn't be revisited says **settled — don't re-pitch** with its reason.
  Heading vocabulary in `docs/claude/`: **Not modeled yet** for absences, **Divergences** for shipped-but-not-byte-identical, and real's-own-error rejections fold into the feature's modeled description rather than any gap list.
- **AssemblyHooks**: each test project's `AssemblyHooks.cs` has a `static [TestClass] [AssemblyInitialize]` warming shared init once before the parallel run.
  Without it, the first test batch races to init hot shared state and serializes on contention.
  The analyzer-tests' Roslyn-cache warm-up is the worst case (~3x slowdown); the pattern generalizes to any expensive first-touch shared resource.

## SimulatedSqlException vs NotSupportedException

- `SimulatedSqlException` for behavior matching SQL Server: invalid SQL, type mismatches, constraint violations, oversize columns, truncation.
  Mirrors number/class/state/message.
- `NotSupportedException` for valid SQL Server features the simulator hasn't built.
  Name the unmodeled feature.
- **Control flow signals via flags, not exceptions.**
  BREAK / CONTINUE / RETURN / THROW set a typed `BatchContext` flag (`LoopControl` + skip predicate), never a signal exception — `yield return` inside try/catch composes badly with iterator dispatch, and the flag reuses the skip-mode plumbing that no-ops un-taken IF branches.
  Parse-time structural checks (BREAK outside WHILE → Msg 135) fire regardless of skip state.
  Exceptions stay for true errors.

## What's modeled

The `*.Tests` and `*.Tests.EFCore` suites are the authoritative behavior contract.
The [Feature reference](#feature-reference) index maps every modeled area to its deep-dive — **presence there means it's modeled**; read the linked doc on demand when working in that area.
The two subsections before it are cross-cutting notes belonging to no single feature.

### EF Core adapter coverage
`UseSqlServerSimulator(...)` covers seven SqlParameter-downcast pairs: `DateOnly→date`, `DateTime→date`, `DateTime→smalldatetime`, `TimeOnly→time(N)`, `TimeSpan→time(N)`, `decimal→money`, `decimal→smallmoney`.
Without the adapter these throw at SaveChanges.
MAX-string family flows through plain `UseSqlServer`.

### Faithful rejections

Raising real SQL Server's own error **is** modeled behavior.
These are coverage, not gaps: accepting the statement instead would regress fidelity, and the simulator accepting what real rejects is the more dangerous divergence direction (see the over-permissive section of [`backlog.md`](docs/claude/backlog.md)).

- `IGNORE_DUP_KEY` where real forbids it: a non-unique `CREATE INDEX` → **Msg 1916**, a filtered unique index → **Msg 10618**, an index on a view → **Msg 1990**, and via `ALTER INDEX … SET` a non-unique index → **Msg 1915** or a constraint-backed one → **Msg 1979** (real accepts the option in a constraint's own declaration but refuses to change it later) — see [`constraints.md`](docs/claude/constraints.md).
- `text` / `ntext` / `image` in comparison (Msg 402), ORDER BY / DISTINCT (Msg 306), aggregates (Msg 8117 from MAX/MIN), and every string scalar's *transformed* argument (Msg 8116 naming type, argument position and function) — LEN / LEFT / RIGHT / UPPER / LOWER / L-R-TRIM / REVERSE / REPLACE / STUFF / REPLICATE / CHARINDEX / PATINDEX / ASCII / UNICODE / SOUNDEX / DIFFERENCE / TRANSLATE / STRING_ESCAPE / STRING_AGG / FORMAT.
  Arguments that are *read* rather than transformed still take one (CHARINDEX's haystack, PATINDEX's subject, SUBSTRING's source, DATALENGTH) — see [`scalars.md`](docs/claude/scalars.md#legacy-lob-arguments).
- `bit` paired with `bit` in arithmetic — **Msg 402** for `+` / `-` / `%`, **Msg 8117** for `*` / `/` (bitwise `&` / `|` / `^` and a mixed `bit + int` stay legal) — see [`arithmetic.md`](docs/claude/arithmetic.md).
- A column list on a permission that has no column form (everything but `SELECT` / `UPDATE` / `REFERENCES`) → **Msg 1020** class 15 state 1, or on a *synonym* securable (entity-level whatever the permission) → **Msg 1020** sev 16 state 3 — see [`permissions.md`](docs/claude/permissions.md).
- `RANGE BETWEEN <N> PRECEDING/FOLLOWING` numeric-offset → **Msg 4194** (real's licensed-feature rejection); `ROWS` numeric-offset ships.
  Default frame with ORDER BY = `RANGE UNBOUNDED PRECEDING TO CURRENT ROW`; without it, whole partition — and LAST_VALUE matches real's default-frame semantic.
  A frame with no ORDER BY → **Msg 10756**, except for `COUNT(*)` / `COUNT_BIG(*)`, which real exempts and frames anyway; a frame as the `OVER` body's only element → **Msg 102** whatever the function, and a named window carrying one → **Msg 5364** with no exemption — see [`query.md`](docs/claude/query.md).
- Table-variable named constraints / FKs → **Msg 102** (real's `DECLARE @t TABLE` grammar restriction); multi-variable DECLARE with a table variable, mixed scalar+table DECLARE, and `SET IDENTITY_INSERT @t ON` reject the same way.
  Column features (IDENTITY / UNIQUE / inline + table-level CHECK / computed / rowversion) all ship — see [`table-variables.md`](docs/claude/table-variables.md).
- `CREATE SCHEMA sys` / `INFORMATION_SCHEMA` and `CREATE TABLE sys.*` / `INFORMATION_SCHEMA.*` → **Msg 2760** (real's permission framing); the schemas exist as catalog-view hosts.
- `ALTER COLUMN` of an IDENTITY column to a non-integer type → **Msg 2749**; of a period column → **Msg 13599** — see [`alter-table.md`](docs/claude/alter-table.md).

Most faithful rejections live in their feature's deep-dive rather than here (the `SELECT … LIMIT n` / `ALTER TABLE … ADD COLUMN` tightenings in [`grammar.md`](docs/claude/grammar.md), the indexed-view gates in [`indexes.md`](docs/claude/indexes.md)); this subsection is only for the ones with no feature-doc home.
Distinguish them from a *gap* that happens to raise: the `SEMANTIC*` rowsets raise `NotSupportedException` because semantic search is unbuilt, which is a not-yet, not a faithful rejection.

### Feature reference

Per-feature deep-dives live under `docs/claude/`.
Each entry below is a trigger: read the linked file on demand when working in the matching area.
Entries name the *area*, not every member — each doc's own headings are its catalog, so grep the linked file before concluding something isn't modeled.

- **Built-in scalars** — the math / date / string / bit families, HASHBYTES / CHECKSUM, FORMAT / FORMATMESSAGE, COMPRESS / DECOMPRESS, the built-in TVFs (`STRING_SPLIT` / `GENERATE_SERIES`), `@@`-constants and statistical counters, the session-state store (SESSION_CONTEXT / CONTEXT_INFO / connection scalars), the `sys.fn_*` system functions, the placeholder security / FILESTREAM scalars, ODBC escape sequences (`{d …}` literals, `{fn …}` calls), and the **shared integer-argument narrowing seam** — an id / position / count / offset outside its parameter's declared range reports SQL Server's own conversion error keyed by source type (Msg 8115 / 220 / 232 / 237 / 248), a system procedure's parameter Msg 8114, a cursor FETCH offset literal Msg 1080, and the bit family's own Msg 8116 / 9838 / 9839 argument rules, plus **`SET DATEFIRST n`** and the exact set of parts that follow it — `DATEPART`'s `weekday` and `week` and `DATETRUNC(week, …)` move, while `DATENAME`'s weekday name, the ISO units and `DATEDIFF(week, …)` don't — with Msg 2742 outside 1..7 and Msg 2743 for a parameter that isn't an `int` → [`scalars.md`](docs/claude/scalars.md).
- **The native `REGEXP_*` family** (SQL Server 2025) — the `REGEXP_COUNT` / `INSTR` / `REPLACE` / `SUBSTR` scalars, the boolean-only `REGEXP_LIKE` predicate, and the `REGEXP_MATCHES` / `REGEXP_SPLIT_TO_TABLE` rowset members, with their argument-validation order, the Msg 19300-19309 diagnostics, and the **RE2 pattern dialect** — the simulator translates a pattern into .NET's `Regex` and raises real's error for everything RE2 rejects (backreferences, lookaround, atomic groups, possessive quantifiers, `(?x)`), rewriting the constructs whose meaning differs (`$` anchoring, the ASCII-only `\d` / `\w` / `\s` / `\b`, POSIX classes, `(?U)`).
  Compatibility-gated: the predicate and both rowset members ship at level **170** only; the four scalars carry no gate → [`scalars.md`](docs/claude/scalars.md#the-native-regexp_-family-sql-server-2025).
- **Legacy LOB operations** — binary `SUBSTRING` (the byte window, the per-source projected `varbinary` width, and the **Msg 536 / 537 split** a negative length takes between a constant one settled while compiling and one that only turns negative at run time, which `LEFT` and `RIGHT` share), the text-pointer scalars `TEXTPTR` / `TEXTVALID`, and the `READTEXT` / `WRITETEXT` / `UPDATETEXT` statements they address rows for — the grammars, the **bytes-for-`text`-and-`image` versus characters-for-`ntext`** unit split, `READTEXT`'s size-0 read-to-the-end and `UPDATETEXT`'s NULL-or-negative append / to-the-end rules, the Msg 182 / 7122 / 7123 / 7124 / 7125 / 7133 / 7116 / 7135 / 518 diagnostics (7133 being what forces the initialize-with-an-empty-value dance), and the deterministic pointer encoding the statements resolve back through a per-table row-binding cache — plus what these statements are *not*: no trigger fires and no `rowversion` advances, while both writes still roll back with their transaction → [`legacy-lob.md`](docs/claude/legacy-lob.md).
- **Type promotion and arithmetic** — `SqlType.Promote` / `PromoteForArithmetic`, the `Storage/Decimal38` backing type (a `UInt128` magnitude + sign + scale over 256-bit intermediates, carrying `numeric`'s whole 38-digit domain), decimal precision-scale (including the 38-cap's per-operator-family scale reduction and the **digit rule the result settles with** — division truncates its quotient toward zero at the result scale, at every cap depth, where every other operator rounds half away from zero, with `money` and `AVG` following, and modulo aligning both operands at the result's scale so an operand past 38 digits there is Msg 8115 state 2), int↔string promotion, integer overflow (Msg 8115, per-type ranges), integer-literal typing (`int` while it fits, then `numeric(digit_count, 0)` — never `bigint`, which only a CAST reaches — plus the negated-constant fold that keeps `-2147483648` an `int`), literal value-width typing and the width algebra → [`arithmetic.md`](docs/claude/arithmetic.md).
- **`Cast` / coercion error paths** — CAST/CONVERT narrow targets, the TRY_CAST/TRY_CONVERT swallow set, the **compile-time conversion-legality table** (Msg 529 settled from the two types, so a typed NULL, an empty rowset, a `TRY_CAST` and a module body at CREATE all raise it; 284 refusals probed across every ordered pair of the 28 common type names, legacy-LOB allow-lists included), PARSE/TRY_PARSE, ANSI type-name synonyms, and the **per-style string→date input grammar** (a date-part order × year-width table per style, with one grammar for the legacy `datetime` / `smalldatetime` targets and a stricter own-layout-only one for `date` / `datetime2` / `time` / `datetimeoffset`; the century restriction, the `T` / `Z` rules, and the Msg 241 / 242 / 295 / 9807 split) → [`casting.md`](docs/claude/casting.md).
- **`SimulatedDbDataReader` client surface** (typed accessors, `datetime` client-millisecond rounding, `GetOrdinal` precedence, GetBytes/GetChars materialization divergence) → [`data-reader.md`](docs/claude/data-reader.md).
- **`Selection`, aggregates, window functions, set ops, CASE, OFFSET/FETCH, `TOP`, named windows, `TABLESAMPLE`, the derived-table column-alias list (`(SELECT …) s(a, b)`, with Msg 8155 / 8156 / 8158 / 8159)** — including the aggregate/GROUP BY binding rules (Msg 130 / 8117 / 144 / 164, via `ParserContext` parse-time counters rather than a tree walk) and the grouped-query window shapes (a window spans the whole grouped result, `ROLLUP` / `CUBE` / `GROUPING SETS` subtotal rows included) and the folded-constant ORDER BY gates (Msg 408 / 1008 on a statement, Msg 5308 / 5309 inside `OVER` / `WITHIN GROUP` / `NEXT VALUE FOR … OVER`, off one probed catalog of the built-ins real folds), plus the **parallel grouped accumulation** — the streaming single-grouping-set path's per-row consumer work forked across worker threads while the calling thread produces the stream, with its engagement gates, its exact-merge-only aggregate list and the on-any-error-re-run-serially rule that keeps error identity byte-identical; built, tested and **off by default** (see the doc for the concurrent-workload measurement that keeps it off) — plus the **arrival-order tiebreak** `ROW_NUMBER` ranking carries (which member of a tie group takes which number is settled by the row's own arrival position rather than left to the sort — real leaves it plan-dependent, and pinning it is what lets the greatest-n-per-group idiom answer a constant `rn` bound from a bounded per-partition selection instead of a full sort of every partition) — plus the **RANGE-frame ORDER BY gate**, **Msg 8728** for a MAX-typed ordering expression under a RANGE frame, written or the default an `ORDER BY` confers on a frame-taking function (`CUME_DIST` included, the rest of the ranking family not) — plus the **GROUP BY containment rule** (Msg 8120 / 8121 / 8127), where a grouping *expression* covers a projection sub-expression that matches it rather than the columns it names, so `SELECT (a + 1) * 2` and `SELECT YEAR(d) * 100 + MONTH(d)` are licensed while `SELECT a`, `SELECT a + 0` and `SELECT 1 + a` are refused, and the message names the object the FROM clause wrote rather than its alias — plus the **row-count operand's binding split**: a value check per execution (any integer-family value or exact numeric at scale 0, everything else Msg 1060), except in a module body, where a name resolves from its *declared type* since binding never runs, so `TOP (@rows)` over an `int` parameter creates while the `nvarchar` / `decimal(5, 2)` spellings and the written `TOP (NULL)` / `TOP (1.5)` are all refused at CREATE — plus **`SET ROWCOUNT n`**, the session-wide sibling of `TOP` that composes with it as a minimum, caps what a statement *emits* rather than what it reads (so `COUNT(*)` still answers over the whole table), reaches every row-emitting shape including the DML family at the one list-trimming seam they share, and turns a `NEXT VALUE FOR` in the same statement into Msg 11739 → [`query.md`](docs/claude/query.md).
- **Subqueries** (EXISTS / IN(SELECT) / scalar / quantified ANY-SOME-ALL, three-valued rules — including a **NULL left side**, which settles on the inner's *emptiness* alone: FALSE against an empty body whatever the values, UNKNOWN against a non-empty one, per correlation key — arbitrary-depth correlation, set ops in subquery contexts), and the **once-per-statement execution** an inner plan that never reads the enclosing row takes — decided by a runtime probe on the plan's first execution (an outer-row resolver that latches when consulted), memoized on `StatementContext` per the shared-plan contract, declined for a plan that drew a `NEWID()` or advanced a sequence (real re-draws those per outer row, probe-confirmed, while `RAND()` and the current-time family are statement-frozen on both engines), and carrying a collation-aware hashed probe set for `IN` that same-family promotion gates so a cross-family pair's per-value conversion error still surfaces in row order.
  Plus the two **decorrelation** transforms: an equi-correlated `EXISTS` / `NOT EXISTS` / `[NOT] IN` past **128** outer rows answers from a **hash semi / anti-join** built by executing the body once with its correlation equalities stripped (per-**key** `sawNull` for the `IN` forms, a NULL key on either side matching nothing, a seekable inner delayed by the join planner's own 4:1 crossover, and the same correlation latch declining a body the parse-time classification couldn't see into), while a small **uncorrelated** `IN` set (≤ 64 values) becomes the equality family an index seek **drives from** → [`subqueries.md`](docs/claude/subqueries.md).
- **Outer-scope correlation from the select list** — the FROM clause binds before the select list (SQL Server's binder order, not the written order) so a select-list subquery, a derived table in a subquery's FROM, or a nested APPLY can reference the enclosing query; a **FROM-less body** installs the inherited scope for its own nested queries, which is what lets an APPLY body's `WHERE EXISTS (… c.k …)` bind → [`query.md`](docs/claude/query.md#outer-scope-correlation-in-the-select-list).
- **JOIN / APPLY** — all join kinds + CROSS/OUTER APPLY, ANSI-89 comma-FROM, EF `LeftJoin`/`RightJoin` routing, `JoinDriver`'s equi-join hash fast path vs nested-loop fallback (whose seek-vs-hash choice is an outright small-outer cap plus an inner-to-outer row ratio), the scope a **non-`APPLY` FROM source's own arguments** bind in — SQL Server holds none of that FROM's sources there, so a generator naming a sibling is **Msg 4104** (its unqualified spelling real's Msg 207) — the **once-per-enumeration materialization of deferred FROM sources** (which sources qualify, the generator-backed ones the 4104 admits, and the `NEWID()` gate), and the **WHERE pushdown into every base-table source** plus the **narrowed-source-first reorder** of a pure INNER equi-join chain (safe for every join kind because the matched conjuncts stay residual, and bounded by a decline list — an outer join, a non-equi or single-source ON conjunct, a disconnected join graph, a deferred source, a wide narrowing) → [`joins.md`](docs/claude/joins.md).
- **`PIVOT` / `UNPIVOT`** — PIVOT desugars to grouped conditional aggregation, UNPIVOT is a NULL-skipping unfold; both attach as a postfix FROM-source wrapper on the derived-table `LateralPlan` seam → [`pivot.md`](docs/claude/pivot.md).
- **UPDATE / DELETE / INSERT…SELECT / SELECT…INTO / MERGE / OUTPUT**, plus rowversion (`@@DBTS`), the identity helpers (`SCOPE_IDENTITY` / `IDENT_CURRENT` / …), `@@ROWCOUNT`, `DEFAULT` as a VALUES element, and the **write-target qualifier rule** — a `SET` assignment target admits only the write target as written (its alias once it has one), everything else being Msg 4104 on the whole dotted form, while a `VALUES` cell has no column scope at all → [`dml.md`](docs/claude/dml.md).
- **Variables, control flow (IF/WHILE/BREAK/CONTINUE/RETURN), TRY/CATCH+THROW+ERROR_*, `@@ERROR` / `@@TRANCOUNT` / `XACT_STATE`, WAITFOR**, the **statement separators real allows between an `IF`'s THEN branch and its `ELSE`** (consumed only when an `ELSE` actually follows), and **`PRINT`** — the coalesced `InfoMessage` delivery, the single-space message NULL and the empty string both produce, the 8000 / 4000-character truncation, the implicit-conversion rendering each non-string family takes (style 0 for the legacy datetime pair and for `float`, ISO for the modern date / time types, `0x` hex for the binary family), and the **scalar-only operand** gate — a name is **Msg 128** naming it as written and a subquery at any depth is **Msg 1046**, whichever the left-to-right reading meets first, both settled while parsing → [`control-flow.md`](docs/claude/control-flow.md).
- **Error diagnostics** — `SimulatedSqlException` / `SimulatedError` line-number rules per context, `Server` / `Procedure` population, `ERROR_LINE` / `ERROR_PROCEDURE` parity, the TDS ERROR/INFO token server field → [`errors.md`](docs/claude/errors.md).
- **Cursors** — the full DECLARE/OPEN/FETCH/CLOSE/DEALLOCATE lifecycle, STATIC / KEYSET / DYNAMIC sensitivity and the scrollability each implies (with the Msg 16911 / 16925 split between a forward-only cursor and a dynamic one refusing ABSOLUTE, and a cursor naming no sensitivity staying forward-only whatever it converted to), GLOBAL / LOCAL scope, cursor variables, the concurrency modes (SCROLL_LOCKS / OPTIMISTIC) and their status scalars, and **multi-source and deferred-source cursors** — a JOIN / comma-FROM / self-join cursor, and one reading through a **derived table, CTE, APPLY right side or view** (to any nesting depth), is DYNAMIC (or KEYSET) like real's, re-folding the live base heaps per FETCH over the flattened tuple of row addresses the whole shape reads, with `WHERE CURRENT OF` addressing the reference **as written** — a view is named by the view (its CHECK OPTION then applies, Msg 550), a query body is transparent and named by its base table, and everything else is Msg 16933 (plus Msg 16947 for a NULL-extended outer-join / OUTER APPLY side or a keyset hole, Msg 16931 for a cursor merely unpositioned, and Msg 16961 for a self-join's first-instance binding).
  **Row-limited cursors** (`TOP n` / `PERCENT` / `WITH TIES` / `OFFSET…FETCH`, on the statement or inside a body the cursor follows) convert to **KEYSET** like real's, the limit choosing membership at OPEN while later FETCHes re-read the frozen key set — except a limit inside a *view* body, which real re-evaluates per FETCH so a member the view no longer returns reports `@@FETCH_STATUS = -2`; a **`FOR SYSTEM_TIME`** cursor is the read-only snapshot real reports, in all five forms.
  An **ORDER BY no index delivers** converts the same way (a sorted result is a materialized one), off a leading-prefix rule that resolves each item down to its base column through ordinals, aliases and view / derived-table bodies, matches it against the table's keys in a consistent direction, and carries a nonclustered index's clustering key after its own.
  A KEYSET reached *any* of those ways then meets the **row-locator gate**: every participating base table has to carry a PK / UNIQUE constraint, an unfiltered unique index (a **disabled** one counts — real reads metadata, not usability) or a clustered index (**non-unique** counts, the uniquifier fixing the row), and a table with none — a bare heap, one with only a non-unique nonclustered or a *filtered* unique index, an IDENTITY or `rowversion` column standing in for neither — converts the cursor to `Snapshot | Read Only`, so positioned DML is **Msg 16929**; one keyless side of a join converts the whole cursor, while DYNAMIC needs no locator and the `sp_cursoropen` **API server cursors are exempt** (real keeps those Keyset and updatable over a keyless table, the split its own `API |` vs `TSQL |` property prefix reports).
  **`TYPE_WARNING`**'s Msg 16956 reports every such conversion, against the sensitivity the keywords *imply* as much as one spelled out → [`cursors.md`](docs/claude/cursors.md).
- **CTE shapes / recursive-CTE error handling**, including the **declared column list scoping the recursive member** (a member reads the CTE's columns under the names `WITH cte (a, b, …)` declares rather than the anchor's own projection names — AdventureWorks' `uspGetBillOfMaterials` family reads `[RecursionLevel] + 1` against an unaliased literal `0` — with the arity mismatch still reported after the whole body binds, so it is Msg 8158 / 8159 rather than a Msg 207 on the name the member read), the recursive member's construct restrictions — DISTINCT (Msg 460), TOP / OFFSET (461), outer join (462), GROUP BY / HAVING / aggregate (467), each covering the member's whole text including nested subqueries — and **where a `WITH` prefix may appear**: on a statement, and on a stored body (view, inline TVF, cursor declaration) through the shared body-parse seam, but never on a parenthesized query (derived table / subquery / scalar UDF `RETURN (…)` / `MERGE … USING (…)` → Msg 156, matching real) — and the **unread-prefix refusal**, **Msg 422** on the one shape real refuses it: a bare `SELECT <expression list>`, any clause at all or a DML target being enough to license the prefix however many of its CTEs go unread → [`ctes.md`](docs/claude/ctes.md).
- **JSON** — the JSON_* scalar family, ISJSON, OPENJSON, the `FOR JSON PATH | AUTO` result-serialization clause, and the **document scan** every one of them shares: only an object or array is JSON text (a root-level scalar is malformed input), and a document the reader can't get through is **Msg 13609** naming the offending character and its zero-based position, whatever the path's lax / strict prefix says — with the reader stopping as soon as the path is settled, so the same document raises for one path and answers for another.
  The same left-to-right reading shows up twice more: a **repeated property name** resolves to its first occurrence everywhere a path resolves (`OPENJSON`'s default schema unfolds instead, so it reports both), and **`JSON_MODIFY` splices the document's own text** rather than reserializing it — every byte the edit didn't touch comes back as written — over the four edits (replace / insert / delete / `append`), each with its own splice point and its own set of no-ops.
  One **path parser** serves them all, taking whitespace between any two of the grammar's tokens (`'  lax  $ . a [ 0 ] '`) and reporting **Msg 13607** with real's offending character, zero-based position and per-position State; `JSON_MODIFY`'s written value is type-gated while compiling (**Msg 8116** for money, the date/time family, `uniqueidentifier`, binary, the legacy LOBs, `xml`, `sql_variant`, `hierarchyid` and the spatial types, a typed NULL included), and every JSON producer escapes `/` as `\/` bar the two strings that aren't a rendered value → [`json.md`](docs/claude/json.md).
- **Name resolution** (including **which error an unresolved column reports** — real splits by *what* failed, so a qualified name whose qualifier names no source in scope is **Msg 4104** on the whole written identifier while a known qualifier's missing column and every unqualified miss are **Msg 207** on the leaf, with an alias shadowing the table name and a single-table `UPDATE` / `DELETE` admitting only its target *as written*), **schema lookup, CREATE SCHEMA's `AUTHORIZATION` owner (`sys.schemas.principal_id`, Msg 15151 / 2759, and Msg 15138 refusing to drop a schema-owning principal) and its `<schema_element>` list (whose unqualified names resolve to the schema being created, all-or-nothing on failure), DROP / ALTER SCHEMA TRANSFER, CREATE / DROP DATABASE (stored smallest-free db_ids with freed-id reuse), the `OBJECT_*` / `SCHEMA_*` / `DB_*` scalars (a **constraint name** resolving through them like any other schema-scoped object, scoped to its table's schema), cross-database reads *and writes* (three-part DML / DDL, with the rowversion counter, version store, trigger dispatch, versioning flags and permission check all following the target database)**, and **synonyms** — `CREATE` / `DROP SYNONYM`, redirection at every reference site (FROM / DML / EXEC / function call), the `sys.synonyms` + `sys.objects` projection and `OBJECT_ID` / `OBJECTPROPERTYEX` reads, deferred base resolution (Msg 5313 / 470 at first use), and the Msg 2714 / 3705 collision rules in both directions → [`schemas.md`](docs/claude/schemas.md).
- **System metadata surfaces** — the `sys.*` / `INFORMATION_SCHEMA.*` views, the **per-object creation-time `ANSI_NULLS` capture** (`sys.tables` / `sys.sql_modules`'s `uses_ansi_nulls` + `OBJECTPROPERTY`'s `IsAnsiNullsOn` / `ExecIsAnsiNullsOn`; metadata only — the `= NULL` comparison semantic real freezes with it isn't modeled), `sys.sql_modules.execute_as_principal_id` (real's `-2` / creator / named-user / NULL encoding) and its **`inline_type` / `is_inlineable`** pair (a probed scalar-UDF-inlining disqualifier subset, compat-level-independent), **`OBJECTPROPERTY`'s `IsDeterministic`** (the schema-binding precondition, the nondeterministic-built-in table, the transitive module walk, and the `CAST` / `CONVERT` **style rule** — a conversion between a date/time type and a character string is nondeterministic unless its style is in the probed deterministic set, the converted expression's own type inferred from the columns, parameters, literals and nested conversions its extent names), the **reported client identity** (`host_name` / `program_name` off the connection string's `Workstation ID` / `Application Name` or LOGIN7, feeding `HOST_NAME()` / `APP_NAME()` and the `sp_who` family), the **per-constraint `create_date` / `modify_date`** (inline shares the table's instant, `ALTER TABLE … ADD` carries the later one, a trust toggle advances `modify_date` alone), the metadata / file / filegroup scalars, the **stable `column_id`** (a monotonic per-table watermark in `sys.tables.max_column_id_used`; DROP COLUMN leaves a permanent hole and ALTER COLUMN / `sp_rename` preserve the id), **computed-column nullability** (`sys.columns.is_nullable` is the *expression's*, off the same inference the COLMETADATA `fNullable` flag reads — `CONCAT` / `ISNULL` NOT NULL, `LEN` / `CAST` / exact-numeric arithmetic nullable — declared on both the CREATE TABLE and ALTER TABLE ADD paths, and what lets WideWorldImporters' `Warehouse.StockItems` adopt its history table), the `xp_*` / `sp_*` system procs backing ODBC and JDBC metadata calls (`sp_datatype_info_100`, `sp_tables`, `sp_columns_100`, `sp_pkeys`, `sp_statistics_100`, `sp_stored_procedures`), **`sp_configure` + `RECONFIGURE`** (the staged-vs-installed value split `sys.configurations` projects), the **`sp_help` family** (`sp_helptext`'s CR+LF line rule, `sp_help`'s per-object-kind result-set sequence, `sp_helpindex`, `sp_helpconstraint`, `sp_helpdb` + its appended `sp_helpfile` set, `sp_helpfile` standalone, `sp_helpstats`, `sp_helprotect`'s object + statement permission report, `sp_helptrigger`, `sp_helpuser`), **`sp_spaceused`** (page counts shared with `sys.dm_db_partition_stats`), the **`sp_who` / `sp_who2`** session lists (live connection registry + the lock manager's blocking spid), **`sp_MSforeachtable`** / **`sp_MSforeachdb`**, and the **expression-dependency surfaces** — `sys.sql_expression_dependencies`, `sys.dm_sql_referencing_entities` / `sys.dm_sql_referenced_entities`, the legacy `sys.sql_dependencies` / `sysdepends` pair (id-based rather than name-based, so an unresolvable reference drops out, and with a narrower object row) and `sp_depends`, all six projected from one walk of each entity's stored definition text (module bodies, DDL triggers, computed columns and CHECK / DEFAULT expressions), which is what reproduces real's name-based refresh rules: a `DROP` of the referenced object leaves the row with a NULL `referenced_id`, recreating it restores the id, an `sp_rename` leaves the stale name, and column rows appear in the catalog view only for a schema-bound reference while the DMV emits them for every module kind, with `is_selected` / `is_updated` / `is_select_all` / `is_insert_all` / `is_caller_dependent` / `is_ambiguous` and the Msg 2020 / 15459 / 15460 / 15461 / 15009 / 15250 diagnostics → [`catalog-views.md`](docs/claude/catalog-views.md).
- **Scalar UDFs / TVFs / views / stored procs / dynamic SQL / `@@NESTLEVEL` / `@@PROCID`**, and the **`ALTER` / `CREATE OR ALTER` replacement path** shared by all four module kinds (identity / grants / view triggers preserved, `modify_date` advanced, a view's indexes dropped; Msg 208 on a missing name and **Msg 2010** on a name an incompatible kind holds — including an `ALTER FUNCTION` that would change the function's own kind), plus **`EXECUTE … WITH RESULT SETS`** (the `UNDEFINED` / `NONE` / explicit-definition forms, the declared names and types reaching both front doors' metadata, the implicit-conversion gate real applies there, and the Msg 11535 / 11536 / 11537 / 11538 / 11553 contract errors attributed to the producing module), plus the **module-body statement freeze** — which batch a body's `GETDATE` / `SYSDATETIME` family reads from: a view / inline-TVF body is inlined and adopts the referencing statement's freeze, while a body that dispatches statements of its own re-stamps per statement — plus the **`WITH SCHEMABINDING` dependency gate**, which pins everything a schema-bound body names: **Msg 3729** on a `DROP` (state 1) or `ALTER` (state 3) of a referenced table / view / function, **Msg 5074** on `ALTER TABLE DROP COLUMN` / `ALTER COLUMN` of a referenced column, **Msg 15336** on `sp_rename`, **Msg 15348** on `ALTER SCHEMA TRANSFER`, and **Msg 4512** / **Msg 4513** on what the schema-bound body may itself reference (two-part names only; referenced modules must be schema bound too), plus **CREATE-time body binding** — a procedure / scalar-UDF / multi-statement-TVF / DML- or DDL-trigger body is bound when the module is created, so a binder error (Msg 207 / 209 / 8120 / 306 / 108 / 156 / 137 / 134 / 135 / 402 / 8144 / 178 / **10700**) aborts the `CREATE` with the module uncreated and an `ALTER`'s previous body left standing — **every** binder error the body carries, as one exception holding an entry each in source order, the way a client sees a multi-error response (a severity-15 parse-phase error still preempts the whole report, as on real), while *missing-object* references still defer (real's deferred name resolution — which is what makes a recursive UDF creatable), plus the **function body-shape rules** a scalar UDF and a multi-statement TVF carry beyond that bind (procedures, triggers and inline TVFs exempt): **Msg 455** (the last *top-level* statement must be `RETURN` — a bare `BEGIN … END` is transparent, a trailing `IF` / `WHILE` never satisfies it), **Msg 444** (a body `SELECT` returning rows to the client, state 2 when it reads a rowset and 3 for a computed projection), and **Msg 443** naming the operator real names — the DML / `TRUNCATE` / `SELECT INTO` / transaction / `SET` family at state 15, `PRINT` / `RAISERROR` / `THROW` / `WAITFOR` / `EXECUTE STRING` / `BEGIN TRY` at 14, and the side-effecting built-ins (`newid` / `newsequentialid` / `rand`) at 1, with a table-variable write staying legal — plus parse-phase **Msg 1075** for a bare `RETURN` in a scalar UDF (the required form in a multi-statement TVF), which preempts the rest of the report; the shape violations are gathered through the bind walk and reported behind the binder's own errors, since real reports every binder error ahead of every shape error — plus **DML through views**, where a single-source body routes to its base table and a body over a **JOIN** takes an `UPDATE` whose SET list — or an `INSERT` whose explicit column list — lands entirely in one base table (join-multiplied targets writing once, the untargeted columns taking their defaults, a derived output column readable in the WHERE but **Msg 4406** to write, `WITH CHECK OPTION` covering the join as well as the body's WHERE, and the positioned `WHERE CURRENT OF` form following), a **chain of single-source views over such a body** composing to any depth (each level's names, WHERE and CHECK OPTION resolved through the level below), while a list spanning two base tables — and a `DELETE`, or an `INSERT` with no column list — is **Msg 4405** as on real, both it and Msg 4406 raised as the left-to-right walk of the list meets them → [`programmable.md`](docs/claude/programmable.md).
- **CLR assemblies** — `CREATE`/`DROP ASSEMBLY`, `CREATE FUNCTION … AS EXTERNAL NAME` scalar routines, the `Simulation.EnableClr` host opt-in, static SAFE verification, `sys.assemblies` / `assembly_files` / `assembly_modules`, `ASSEMBLYPROPERTY` → [`clr-assemblies.md`](docs/claude/clr-assemblies.md).
- **`#foo` / `##foo` routing, DROP TABLE, TRUNCATE TABLE** → [`temp-tables.md`](docs/claude/temp-tables.md).
- **`DECLARE @t TABLE`, table-variable DML, `OUTPUT … INTO`** → [`table-variables.md`](docs/claude/table-variables.md).
- **`CREATE TYPE … AS TABLE`, TVP params + `READONLY`, ADO.NET TVP** → [`table-valued-parameters.md`](docs/claude/table-valued-parameters.md).
- **`CREATE TYPE … FROM <builtin>` (scalar alias types / UDDTs), multi-part type-references** → [`alias-types.md`](docs/claude/alias-types.md).
- **`sp_addextendedproperty` / `fn_listextendedproperty` / `sys.extended_properties`** → [`extended-properties.md`](docs/claude/extended-properties.md).
- **`CREATE/ALTER/DROP SEQUENCE`, `NEXT VALUE FOR`, `sys.sequences`**, and the **nine-message family** a reference is refused by — **11719** a nested query or stored expression (a derived table / CTE / subquery / `APPLY` body / view or function body, at `CREATE` attributed to the module, plus a `CHECK` constraint and a computed column), **11725** an aggregate's argument, **11721** a statement carrying `DISTINCT` or a set operator, **11723** one carrying an `ORDER BY` the reference names no `OVER` against, **11720** the eight query clauses, **11739** a `TOP` or `OFFSET`, **11741** the `CASE` / `IIF` / `COALESCE` / `ISNULL` / `NULLIF` family (which real's message names `CHOOSE` in and yet exempts), **11742** a `MERGE` action, **11738** `PRINT` — reported in that precedence order where two apply at once, with a joined `UPDATE` / `DELETE`'s own FROM-clause derived table exempt as on real; every refusal is settled at parse, so the sequence never advances → [`sequences.md`](docs/claude/sequences.md).
- **DML + DDL triggers** (`CREATE TRIGGER` incl. `ON DATABASE`, `INSERTED`/`DELETED`, `TRIGGER_NESTLEVEL`, `sys.triggers`), **DDL-trigger firing** (a database-scope trigger runs after the modeled CREATE / ALTER / DROP statement completes, with `EVENTDATA()` returning the `<EVENT_INSTANCE>` document and grouped declarations like `DDL_TABLE_EVENTS` matching through their leaf-event closure), the **trigger atomic scope** (the body has none of its own — the firing statement and everything its triggers wrote roll back as one unit, including writes by modules the body called) and **Msg 3616** (a severity-≥11 error the body's own TRY/CATCH swallowed still aborts), the **`UPDATE(col)` / `COLUMNS_UPDATED()`** change-detection intrinsics, **trigger-body result sets** (a body `SELECT` is the firing statement's result set; the body's rows-affected stays suppressed), **Msg 334** (a client-returning `OUTPUT` can't target a table with an enabled trigger for that action), **`sp_settriggerorder`** firing order with its `OBJECTPROPERTY(… 'ExecIsFirst/LastInsert/Update/DeleteTrigger')` read-backs, and the two **nesting options** — per-database `RECURSIVE_TRIGGERS` (direct self-recursion, innermost frame only, Msg 217 at the 32-level cap) and the `nested triggers` server option (only the first AFTER level fires; INSTEAD OF exempt) → [`triggers.md`](docs/claude/triggers.md).
- **CHECK / PRIMARY KEY / UNIQUE enforcement** (Msg 547 / 8141 peer-reference gate, Msg 2627 vs 2601, NULLs-equal UNIQUE), the **computed-column rules** (a PERSISTED computed column carries a CHECK in every declaration form and is promoted to NOT NULL by a PRIMARY KEY naming it; a non-persisted one raises Msg 8183 from the inline form and Msg 1764 — which beats Msg 8141 — from the rest), the **Msg 4936 determinism gate** `PERSISTED` takes at every declaration site (reusing `OBJECTPROPERTY`'s `IsDeterministic` walk, conversion-style rule included, and exempting a `type::Method(…)` static call from the same-named built-in), **`IGNORE_DUP_KEY`** (duplicate-row skip + the once-per-statement severity-0 Msg 3604, `ALTER INDEX … SET`, and the Msg 1916 / 1915 / 10618 / 1990 / 1979 rejections), and the **seek-backed duplicate detection** behind it — enforcement shares the per-`Heap` seek cache with reads and FK checks, declining to a scan for a NULL key component, and skipping an UPDATE row whose key stood still → [`constraints.md`](docs/claude/constraints.md).
- **`FOREIGN KEY` + referential actions, `sys.foreign_keys`**, the **child triggers a referential action fires** (a cascade is part of the firing statement, so the child's AFTER DELETE / UPDATE trigger runs at the statement's own `TRIGGER_NESTLEVEL()`, once per table with every cascaded row in one invocation, deepest-first and ahead of the named table's own trigger, seeing the cascaded row count as `@@ROWCOUNT` while the statement's own stays the parent's — and the rewrite recomputes the child's computed columns, which a PERSISTED one needs to avoid keeping its pre-cascade value on disk), the **mutual exclusion** of a `CASCADE` and an `INSTEAD OF` trigger over the *same verb* on one table (**Msg 2113** from the trigger side, **Msg 1787** from the constraint side — action-matched and `CASCADE`-only, so `SET NULL` / `SET DEFAULT` and a cross-verb pairing both coexist, as Insite.Commerce's own `Brand_Instead_Of_Delete` beside an `ON DELETE SET NULL` foreign key requires), and the **computed-column rules** — a PERSISTED computed column is a legal referencing (and referenced) column, a non-persisted one raises Msg 1764 from the table-level / `ALTER TABLE` forms and Msg 8183 from the inline one, and the referential actions that would have to write it raise Msg 1765 / 1715 → [`foreign-keys.md`](docs/claude/foreign-keys.md).
- **`PERIOD FOR SYSTEM_TIME`, history sibling, `temporal_type`, and all five `FOR SYSTEM_TIME` query forms** — `ALL` / `AS OF t` / `BETWEEN t1 AND t2` / `FROM t1 TO t2` / `CONTAINED IN (t1, t2)`, with the boundary rule each carries, the literal-or-variable argument grammar (Msg 102 / 156) and its type gate (Msg 206 / 402), and the zero-duration versions every form hides.
  Also the history-table wiring: **auto-named siblings** (`MSSQL_TemporalHistoryFor_<object_id>` in the base's schema, with a hex suffix on collision), the **shape validation** that adopts an existing history table (Msg 13514 / 13574 / 13515 / 13516 / 13517 / 13518 / 13523 / 13524 / 13525 / 13526 / 13531, in real's check order), the re-issue split on an already-versioned base (Msg 13595 / 13596 / 13757), and **`HISTORY_RETENTION_PERIOD`** — parsed on both DDL paths, projected through `sys.tables`, and enforced as a read-side filter that hides aged versions from every `FOR SYSTEM_TIME` form.
  Plus the **history cleanup index**: an engine-*built* sibling carries the non-unique clustered `ix_<history table>` on `(period end, period start)` real builds with it (an *adopted* table keeps whatever indexing it had), a finite retention period is refused without a clustered index leading on the period end column — **Msg 13765**, state 1 for no clustered index and state 2 for one leading elsewhere — and `DROP INDEX` of that index while the base is on a finite period is **Msg 13766** → [`temporal-tables.md`](docs/claude/temporal-tables.md).
- **`ALTER TABLE` ADD/DROP/ALTER COLUMN + CONSTRAINT (incl. trust toggling)** — with the **multi-element `ADD`** constraint list and its all-or-nothing rollback, the `ALTER COLUMN … { ADD | DROP } { ROWGUIDCOL | SPARSE }` markers (Msg 4924 / 4925 / 4926 / 2761 / 1731 / 4928 / 11410), **`ADD PERIOD FOR SYSTEM_TIME`** (both columns become GENERATED ALWAYS AS ROW START / END and the period is live at once, behind real's own check order — Msg 13597 / 4924 / 13501 / 13587 / 13513 / 13575) and **`DROP PERIOD FOR SYSTEM_TIME`** (Msg 13592 while versioning is on, Msg 13593 with no period), **`REBUILD`** (accept-and-no-op behind real's option validation, with the Msg 155 / 7729 / 7735 partition-number family), and the **DEFAULT expression's empty scope** — a name inside one is **Msg 128** and a subquery **Msg 1046**, settled while parsing so an empty table and a name that *is* a column report identically → [`alter-table.md`](docs/claude/alter-table.md).
- **`CREATE INDEX`** (UNIQUE / CLUSTERED / INCLUDE / filtered — with **Msg 10601** for an INCLUDE list on a clustered index, ahead of every name-resolution error and of Msg 1916, and **Msg 10609** for a filter whose predicate reads a computed column, persisted or not, which real refuses because membership would turn on a value it re-derives), inline indexes in CREATE TABLE, indexed views and their creation gates, `ALTER INDEX SET` / `DISABLE` / `REBUILD` / `REORGANIZE` and the disabled-index rules (unenforced uniqueness, the Msg 8655 clustered lockout, Msg 1973 / 1505 — and `ALL … REORGANIZE` stepping past a disabled index where naming it raises), the resumable trio's own refusal (`RESUME` / `PAUSE` / `ABORT` never have an operation to address, so they are Msg 10638 at real's State 1 / 2 split, or Msg 10680 at Level 11 for `ALL`), `sys.indexes`, the **computed column as an index / constraint / statistics key** (a non-persisted one is legal and enforced — AdventureWorks' `AK_SalesOrderHeader_SalesOrderNumber` — behind real's two preconditions, **Msg 2729** for a nondeterministic expression and **Msg 2799** for an imprecise one, where imprecise means any `float` / `real` anywhere in it and so survives a narrowing `CAST`; both gate a non-unique index and `CREATE STATISTICS` alike, and both append real's constraint suffix when reached through one, while `PRIMARY KEY` stays Msg 1711 — with the key read off an evaluated full row and the existing-row side scanned once per statement into a hash set, since no seek reaches a value with no storage slot), **`CREATE` / `DROP STATISTICS`** (the declaration rather than a histogram — `sys.stats` / `sys.stats_columns` rows with `user_created = 1`, a `stats_id` drawn from the same per-table sequence the index ids use, `NORECOMPUTE` the one observable WITH option, and Msg 1927 / 1088 / 1911 / 3701 where real raises them), `DBCC SHOW_STATISTICS`, and the **access-path choices** a read makes over the per-`Heap` seek cache — the equality / IN / cross-column-`OR`-union / range / equality-prefix-then-range seeks, ORDER BY elimination, the range seek's **span gate** (an interval selecting more than a quarter of a table of 1024+ rows abandons the seek mid-walk for the scan, since past that the per-address reads lose to the sequential one) and the **scan prefilter** (a joined source no key can seek still has its own sargable conjuncts applied to its row stream before the join, which is what lets a range on an unindexed column shrink the join's input) → [`indexes.md`](docs/claude/indexes.md).
- **Table hints (`WITH (NOLOCK …)`) + statement `OPTION (…)` hints**, including **`FORCESEEK`'s nested form** (its index name validated like an `INDEX` hint's, its seek-column list required to be a leading prefix of that index's key columns — Msg 365 for too many, Msg 362 naming the first that isn't, both naming the base table rather than the alias) and the **legacy no-`WITH` parenthesized form**, which splits on the alias: with one written an unknown name is Msg 321, without one real reads the parens as an argument list and reports a **Msg 207 per name** (the source is not in scope for its own arguments) closed by **Msg 215**, while an `INDEX` hint written that way is **Msg 1018** either way → [`query-hints.md`](docs/claude/query-hints.md).
- **Heap page lifecycle** (reclamation/reuse, tail-only shrink, `DBCC SHRINKDATABASE`/`SHRINKFILE`, the maintained slot total `Heap.RowCount` and the four seams that move it, the reused encode buffer and which callers may take it) → [`heap-storage.md`](docs/claude/heap-storage.md).
- **Per-`Simulation` plan cache and token memo** — the two reuse layers over a repeated `CommandText`. The plan cache stores a batch's `Selection` **sequence** (several top-level SELECTs, trailing semicolons tolerated, eligibility settled by counting dispatched statements against collected plans), with the four-part cache key, `SchemaVersion`-stamped invalidation, the inline-in-SELECT-arm promotion seam, and the shared-plan contract; the token memo stores the tokenized form of *any* text — keyed by the four inputs the tokenizer reads, needing no invalidation because tokenization is a pure function of them — which is what serves the statement kinds that have **no plan artifact at all**, DML above all, since `INSERT` / `UPDATE` / `DELETE` / `MERGE` parse and execute in one interleaved pass → [`plan-cache.md`](docs/claude/plan-cache.md).
- **Transactions** (statement atomicity, undo log, BEGIN/COMMIT/ROLLBACK/SAVE, `@@TRANCOUNT`, identity/rowversion log bypass, temp-table logging asymmetry, and `WITH MARK`'s two observable halves — Msg 3901 for an unnamed marked transaction and the severity-10 Msg 3920 for a second mark on one already marked), plus the **transaction-aborting error class** — the rare error that rolls the session's *whole* stack back before anyone sees it, refuses a `BEGIN TRY` frame and ends the batch, against the far commoner statement-aborting one that leaves `@@TRANCOUNT` alone (Msg 8728 is the modeled member; the neighbouring 207 / 208 / 306 / 4104 / 4194 / 8120 / 8134 all leave the transaction standing), plus **`SET XACT_ABORT`**, which generalizes that plumbing conditionally without being the same shape: uncaught, a statement-terminating run-time error ends the batch and rolls the whole stack back, but a `TRY` frame *anywhere on the session stack* still catches it and gets a **doomed** transaction instead (`XACT_STATE()` −1, Msg 3930 on the next write of any kind, Msg 3998 and a rollback at end of batch), with `RAISERROR` the one raising construct the option leaves alone where `THROW` is promoted like everything else → [`transactions.md`](docs/claude/transactions.md#set-xact_abort).
- **Locking, MVCC, SNAPSHOT/RCSI, deadlock/timeout, lock-related DMVs** — including the **instance-wide commit-Xid sequence** (real's server-scoped transaction sequence number): a SNAPSHOT transaction fixes one stamp at its first data-access statement and reads *every* database as of that instant, while RCSI re-stamps per statement, and both the versioning flags and the Msg 3952 gate follow the table's own database rather than the session's.
  Plus the **`NOWAIT`** hint's zero lock timeout, scoped to the table it names, and the **`READPAST`** skip beside `UPDLOCK` / `XLOCK` as well as on the read-committed probe path.
  Plus **key-range locks** — a SERIALIZABLE / `HOLDLOCK` reader fences the *key-space interval* its predicate pins on a **leading key prefix** (`=` / `>` / `<` / `BETWEEN` / `IN`) rather than real's key-anchored gaps, so phantom protection is exact while two transactions over disjoint ranges stop blocking each other; a composite prefix fences the **tuple interval** compared lexicographically (`a = 1 AND b BETWEEN 2 AND 5` admits an insert of `(2, 3)` and blocks `(1, 3)`, while `a = 1` alone leaves `b` open), the four `RangeS-S` / `RangeS-U` / `RangeX-X` / `RangeI-N` modes with their own compatibility family — `UPDLOCK` / `XLOCK` under SERIALIZABLE fencing in `RangeS-U` / `RangeX-X` on top of the row lock they keep — a writer-side `RangeI-N` probe that runs **whatever the writer's own isolation level** (including the post-update image, which is the only thing that reveals a row moving *into* a fence), the `KEY` + mode `sys.dm_tran_locks` projection, and the whole-table S every other read shape still falls back to.
  Plus **abandoned-session reclamation** — a connection an application dropped without disposing has its session torn down through its own `Close` + `Dispose`, so the open transaction rolls back, its locks and `##temp` tables and session application locks release and the SPID retires, the way real's server resets a session once SqlClient's finalizer closes the socket. What makes the connection collectable at all is the one-way **`SessionToken`**: every shared structure — a lock hold, a `##temp` ownership stamp, the active-SNAPSHOT registry, the session list — names the token, and only the token reaches the connection, weakly → [`locking.md`](docs/claude/locking.md).
- **Application locks** (`sp_getapplock` / `sp_releaseapplock` / `APPLOCK_MODE` / `APPLOCK_TEST`, return-code-vs-raised-error asymmetry, EF 9/10 `Database.Migrate()`'s `__EFMigrationsLock`) → [`app-locks.md`](docs/claude/app-locks.md).
- **`hierarchyid` type** (OrdPath storage, byte-identical CAST/wire/DATALENGTH, tier table + opaque passthrough) → [`hierarchyid.md`](docs/claude/hierarchyid.md).
- **`GRANT` / `REVOKE` / `DENY`** — securable resolution and covering/scope-walk enforcement across the DML, EXECUTE and DDL gates, role closure, DENY-beats-GRANT, ownership chaining, module `WITH EXECUTE AS` (whose `USER = 'dbo'` target takes the ordinary IMPERSONATE gate like any other, admitted by a sysadmin / `db_owner` / explicit grantee and Msg 15517 otherwise), and grant-option delegation; the reads of a **subquery that owns its own read list** — a scalar UDF's value-form `RETURN (SELECT …)`, a `SET` / `DECLARE` initializer, an `IF` / `WHILE` condition, a CTE body, a `MERGE … USING` source — checked where its plan runs, since it reaches none of the per-statement check sites; column-level grants on tables and views (Msg 230 naming the view's *own* column); **reference provenance** — a name written through a synonym is checked against the synonym, never the base object behind it; principal and login DDL, server roles and server-scope grants, and the principal scalars.
  **Every modeled CREATE / ALTER / DROP statement carries its own gate** — the create pair (a db-scope CREATE permission then schema ALTER, in whichever order real checks them), the ALTER-shaped gate (object ALTER, which schema ALTER / object CONTROL cover) that `ALTER TABLE` / `ALTER SEQUENCE` / index DDL / `sp_rename` / an `ALTER` or `CREATE OR ALTER` of an existing module take, the DROP-shaped one (schema ALTER **or** object CONTROL, never a plain object ALTER), the parent-object ALTER a DML trigger's three verbs answer to, `ALTER ANY ROLE` / `ALTER ANY SCHEMA` / `ALTER ANY DATABASE DDL TRIGGER` / `ALTER ANY FULLTEXT CATALOG`, database `ALTER` for `ALTER DATABASE`, and the server-scope `CREATE ANY DATABASE` / `ALTER ANY DATABASE` / `dbcreator` pair behind `CREATE` / `DROP DATABASE` — each with its own probed refusal (Msg 262 / 2760 / 1088 / 3701 / 218 / 2104 / 5011 / 7641 / 7666 / 15151 / 15225 / 15247, at the state real reports), and with `db_ddladmin` passing all of it except role DDL and `ALTER DATABASE`.
  Server scope carries both securable classes — the ON-less / `ON SERVER::` class 100 and the `ON LOGIN::` class 101 — and enforces them at four points: DMV server-state gating, `EXECUTE AS LOGIN`'s IMPERSONATE gate, `sys.server_principals` / `sys.sql_logins` row visibility, and login DDL's `ALTER ANY LOGIN` gate.
  **Application roles** (`CREATE` / `ALTER` / `DROP APPLICATION ROLE`, `sp_setapprole` / `sp_unsetapprole` and its cookie, the Msg 505 database pin, `sys.database_principals` type `A`) swap the session's database principal while leaving the login intact.
  **Cross-database references resolve the login's user in the *target*** — its grants answer and a denial names that database (**Msg 229**), a login with no user there is **Msg 916**, and `USE` asks the same question and rebinds the session's user on success; a **catalog-view** read of another database asks it too, filtering by that principal's own visibility and refusing every view (`sys.databases` included) with the same Msg 916.
  The two widening options gate the rest: a database-scoped identity (`EXECUTE AS USER`, any of a module's `WITH EXECUTE AS` frames, an application role) crosses only out of a **`TRUSTWORTHY`** source, where its own login then answers, and an ownership chain crosses only with **`DB_CHAINING`** on in both databases — neither lending *access*, so a login with no user in the target is Msg 916 either way.
  A module's `WITH EXECUTE AS OWNER` / `SELF` frame is database-scoped **despite resolving to `dbo`**, so its data, catalog, `USE` and three-part-`OBJECT_ID` references out of a non-trustworthy source are all Msg 916 (real refuses them even for an `sa` session) while everything it does at home is untouched.
  The `OBJECT_*` scalars ask in the database the argument names, split by form: `OBJECT_ID('other.dbo.t')` resolves first and then gates like a catalog read (Msg 916 for a login with no user there, NULL when the name matched nothing), while the id-form `OBJECT_NAME` / `OBJECT_SCHEMA_NAME` ask visibility alone and answer NULL rather than raising.
  **dbo bypasses every check** → [`permissions.md`](docs/claude/permissions.md).
- **Full-text search** — `CREATE FULLTEXT CATALOG` / `INDEX` and their catalog views, FULLTEXTCATALOGPROPERTY / FULLTEXTSERVICEPROPERTY, and the **query pipeline**: `CONTAINS` / `FREETEXT` and the `CONTAINSTABLE` / `FREETEXTTABLE` rowsets (`KEY` typed off the key index, `RANK`, `top_n_by_rank`).
  The whole `contains_search_condition` grammar ships — term, phrase, per-word prefix inside the quotes, AND / OR / AND NOT with their `&` / `|` / `&!` spellings, `NEAR` in both forms (the distance counts *intervening* terms, stopwords included, and the ordered form takes `MAX`), `FORMSOF(INFLECTIONAL | THESAURUS, …)` and `ISABOUT` — over a probe-anchored English word breaker (interior apostrophe joins, hyphen / underscore compounds into composite-plus-parts, case always folded but accents only for an accent-insensitive catalog), the exact 154-entry system stoplist (an ignored word collapses its AND / AND NOT and raises the severity-10 **Msg 9927**), and a rule-plus-irregulars inflectional stemmer.
  Errors are **Msg 7601** (state 2 table / state 3 column), **7645**, **7630** (states 1 / 2 / 3) and **1046** outside a query scope, at real's own binding times — the column gate at parse, a literal condition at statement compile, a module body's condition deferred to `EXEC`.
  Searches read the live rows rather than a crawled index, so a write is searchable immediately where real's `CHANGE_TRACKING AUTO` lags seconds, and an `xml` column contributes its content but not its markup the way real's does; `RANK` values are the simulator's own → [`full-text.md`](docs/claude/full-text.md).
- **`xml` type, XML schema collections** — including the **typed write**, which validates an instance against the collection its target is bound to and stores it in **canonical form** (`<Total>1.00</Total>` becomes `<Total>1</Total>`, and the canonical text is what a trigger's `INSERTED` and an `OUTPUT` projection read, since the rewrite happens on the way in): a per-primitive renderer whose approximate pair follows XQuery's `fn:string` rule at SQL Server's own 15 / 7 significant digits rather than XSD's canonical form, and an eight-message diagnostic family carrying real's own location trail (**Msg 6926** an invalid simple value, **6965** an element the model didn't want here, **6923** one occurrence too many, **6908** a parent that ended early, **6905** / **6906** the attribute pair, **6913** an undeclared root, **6909** text in element-only content) — run per **assigned value** rather than per row, so an UPDATE that never names the column re-checks nothing; plus the **XML methods** (`.value()` / `.nodes()` / `.query()` / `.exist()` via an XQuery-subset evaluator — location steps with **value, existence and positional predicates**, each step's per-context-node sequences folded into one **document-ordered, duplicate-free** result (which is what puts `//b` in document order rather than level order, and reports `/r/a/..`'s shared parent once), the general (`=` `!=` `<` …) and value (`eq` `ne` `lt` …) comparison operators with XQuery's untyped-atomic coercion rule (`[@x=1]` compares numerically, `[@x="1"]` by code point) and the existential semantics that make `!=` not the complement of `not(=)`, `and` / `or` / `not()`, arithmetic, the built-in function library, and the **binding constructs** — FLWOR (`for` / `let` / `where` / `[stable] order by` / `return`, several bindings per clause, nested and reachable from every method), the quantified `some` / `every`, the conditional `if … then … else …`, `$`-variable references with innermost-binding shadowing, and the constructors, direct (`<out a="{…}">{…}</out>`) and computed (`element name {…}`, whose braced-name form real refuses outright with Msg 9315, as it refuses the computed comment / processing-instruction forms with Msg 9326 / 9325) — over a **CONTENT-typed value model** that admits the fragment real's `xml` admits (several top-level elements, top-level text), reading it at real's own document-node context while a single-root instance keeps the document-element context a `.nodes()` row resolves against — each diagnostic bracketed with the **receiver's own dotted name** — the FROM clause's object as written with any alias ignored (a CTE by the CTE, a synonym by the synonym, a derived table by its alias, a `.nodes()` row column by the column it came from, a variable by nothing at all), which is `FromSource.WrittenObjectName`'s rule shared with the GROUP BY containment messages — carrying the diagnostics real settles **statically**, so they fire while the SQL statement parses and over an empty rowset: Msg **2203** (a predicate that is neither numeric, boolean nor a node sequence), **2204** (a *condition* — an `if` test, a `where`, a `satisfies`, an `and` / `or` / `not()` operand — that is neither boolean nor nodes, so a numeric one is refused where a predicate would read it as a position), **2210** (a sequence or `if` mixing nodes with atomic values), **2227** (an unbound `$`-variable), **2234** (incompatible operand types), **2371** (`position()` / `last()` outside a predicate), **2373** (a constructed node in `.value()` / `.nodes()`), **2389** (a value comparison, a singleton-parameter function, an `order by` key or `.value()` itself over a path real types as a sequence — over untyped `xml` only positional narrowing at *every* step satisfies it, so `(/r/a)[1]` reads where `/r/a[1]` raises, while an `xml(<collection>)` binding — carried by a column, by a `DECLARE @x`, and through the row column a `.nodes()` produces — narrows a named child step the collection declares at most once, which is what lets AdventureWorks' own `Person.vAdditionalContactInfo` read `(act:telephoneNumber)[1]/act:number`), **2395** (an unknown function), **2229** (an undeclared prefix), **2205** / **2209** / **9303** / **9332** (the syntax-error family, which real splits by construct) and **9335** (`at` / `as` / `empty greatest` / `collation` and the operator words it parses but refuses) — plus **`.modify()` XML-DML** — `insert` / `delete` / `replace value of` over the same paths, legal only as `SET @x.modify(…)` or an UPDATE's `SET col.modify(…)` where it desugars to an ordinary column assignment so OUTPUT / triggers / `@@ROWCOUNT` follow (the mutator's target being the write target as the statement wrote it, never a same-named FROM source — AdventureWorks' `Person.iuPerson` writes `Person.Person` past an `inserted` carrying the same column), with the `with` clause a **whole XQuery expression** compiled through the read methods' own engine and carrying the two `sql:` accessors (`sql:column` in its multi-part form, bound against the statement's full scope since the SET list parses ahead of the FROM clause) — which is what evaluates `Sales.iduSalesOrderDetail`'s `data(…)[1] + sql:column("inserted.LineTotal")` — and a **typed element** legal as a `replace value of` target where the untyped spelling is Msg 2356, off the simple-content elements the receiver's `xml(<collection>)` binding declares, with real's compile-time static target typing behind Msg 2226 / 2240 / 2249 / 2337 / 2356 / 2264 / 2207 / 2258, the position errors Msg 8137 / 8113 / 258 / 5302, the Msg 6305 / 2209 / 6306 split between text that parses as XQuery but isn't XML-DML, text that doesn't parse at all and an empty argument, the **attribute node order** an `insert attribute` threads into (an instance's own attributes at ordinals 1, 3, 5, … and the *i*-th inserted one at 2*i*, so `<a m n o p/>` plus `(z, y)` is `m z n y o p`), the **fragment** an `insert … before | after` on the outermost element produces, and the normalized re-serialization an edited instance comes back in), XML indexes (with real's dedicated per-table **256000+ `index_id`** range, which `sys.index_columns` and the internal node table's name follow), and the `FOR XML` result-serialization clause — including **EXPLICIT mode's universal table** (the `Tag` / `Parent` protocol that builds the tree from consecutive rows in the order they arrive, the `ElementName!TagNumber!AttributeName[!Directive]` column convention with the whole directive set — `element` / `elementxsinil` / `xml` / `xmltext` / `cdata` / `hide` / the identity family — and Msg 6801 / 6802 / 6803 / 6804 / 6805 / 6806 / 6807 / 6812 / 6813 / 6815 / 6817 / 6820 / 6824 / 6825 / 6826 / 6827 / 6833 / 6834 / 6835 / 6859 / 3625), the **once-each option rule** (a repeated `TYPE` / `ROOT` / `ELEMENTS` / `BINARY BASE64` is Msg 102 against the clause's own `XML` keyword), the **XML-name split** (RAW / AUTO escape a name XML can't carry as `_xHHHH_`, off the XML 1.0 fourth-edition `Name` production real classifies by, while PATH's aliases and the explicit `RAW('elem')` / `PATH('row')` / `ROOT('name')` arguments reject it — EXPLICIT does neither and writes them verbatim: Msg 6850 / 6846 / 6867 / 6849), **PATH's node functions** (`text()` / `data()` / `node()` / `*` / `comment()` / `processing-instruction(target)`, matched ordinally with no namespace prefix, each taking a path prefix and keeping its sibling position, with the comment / PI constructors writing their value unescaped and Msg 6853 / 6854 / 6879 / 9322 on the rest — `@*` is an attribute named `*`, not a node function), the **`WITH XMLNAMESPACES`** prefix (which element each mode declares the bindings on, the prefixed names it legalizes, and Msg 6868–6874), the **`BINARY BASE64`** option and the legacy `dbobject/TABLE[@PK='V']/@COLUMN` reference AUTO writes without it (Msg 6829 / 6830 / 6831), **AUTO over a set-operation result** (flattened to one level named after the first branch's first source), and the **Msg 6819** refusal of the clause on the SELECT an `INSERT … SELECT` / `SELECT … INTO` / `SELECT @v = …` consumes (FOR JSON's own Msg 13602, except the assignment form where real reports FOR XML's error for both).
  Plus the legacy rowset the same doc carries: **`OPENXML`** and its `sp_xml_preparedocument` / `sp_xml_removedocument` pair — a session-scoped document store handing out real's odd, never-reused handles (Msg 8179 for one this session doesn't hold, Msg 6602 for a document that won't parse), the flags-driven default column mapping (attribute- / element-centric, and bit 8's not-consumed `@mp:xmltext` overflow), the `WITH (col type 'colpattern')` and `WITH <table>` schema forms with the `@mp:` metaproperty family, and the nine-column **edge table** the no-`WITH` form projects, carrying real's own node numbering (the document element is 0 and consumes a slot, a first-child text node swaps with the node numbered next, attribute value text nodes come last).
  OPENXML's patterns are XPath 1.0 — the dialect MSXML gives it — so both the rowpattern and every colpattern run through the DOM's own engine rather than the XQuery-subset translator, with Msg 6603 for one it refuses → [`xml.md`](docs/claude/xml.md).
- **`geography` / `geometry` types** — the parsed value model (WKT read/write with real's 24xxx validation failures, canonical rendering, per-value SRID, Z / M, EMPTY), the UDT serialization it stores and the OGC / ISO well-known binary `STAsBinary` produces, the `Parse` / `ST<Kind>FromText` / `ST<Kind>FromWKB` / `Point` constructor family, and the structural member surface with real's property-vs-method form split (Msg 6592 / 6506 / 6595) — plus spatial indexes and the catalog views.
  All three measures evaluate for both types: `geometry`'s planar `STArea` / `STLength` / `STDistance`, and `geography`'s along the **great elliptic arc** — the curve real measures along, which is not the geodesic and differs from it by metres on oblique intercontinental paths.
  Round-earth `STArea` is a Green's-theorem line integral of the ellipsoid's own zone function around great-elliptic edges (the poleward bulge of a "horizontal" edge is what separates real's answer from the parallel-bounded quadrangle), and `STDistance` is the closest approach between instances of any shape for both types — crossing or containment reads 0, and the round-earth search minimises along the arcs after a chord pre-pass picks the pairs.
  Residuals against real are real's own approximation: ~6e-9 on length, 1e-10 or better on ordinary polygon areas, degrading where an edge spans a large longitude range near a pole.
  **Topological predicates evaluate for both types**, over two hand-rolled DE-9IM engines of the same shape — every edge noded against every other, then the arrangement's nodes, edge pieces and adjacent faces classified against both operands, with the named predicates as masks over the resulting matrix.
  `geometry` gets the **eight** predicates plus `STRelate` (real's non-symmetric `STCrosses` dimension gate and its empty/empty `STEquals` exception included), with orientation in `double` under a roundoff filter, which is what real's own near-collinearity answers show.
  `geography` gets the **six** real exposes there — `STIntersects` / `STContains` / `STWithin` / `STDisjoint` / `STEquals` / `STOverlaps` — over great elliptic arcs, where two edges meet only where their central planes cut the surface, sidedness is a step off the edge rather than a determinant, and containment is the **parity of boundary crossings** from a point a step off a ring's **left**, which is where a geography ring puts its interior (so a clockwise square is the whole globe less itself, and an azimuth-sum winding — which reads the same for a region and its antipodal image — won't do).
  `STTouches` / `STCrosses` / `STRelate` / `STIsSimple` / `STCentroid` / `STPointOnSurface` / `STEnvelope` aren't members of `SqlGeography` at all and report **Msg 6506**, as `EnvelopeAngle` / `EnvelopeCenter` / `NumRings` / `RingN` / `ReorientObject` do on a `geometry` receiver.
  Each type's own **derived points** evaluate: `geometry`'s `STCentroid` (the ring moment sum, NULL for every kind but Polygon / MultiPolygon — a collection of polygons included), its `STPointOnSurface` (real's own pick is the centroid of the **ear at the exterior ring's topmost-then-rightmost vertex**, from the MultiPolygon member reaching furthest right, with a line reporting its *first segment*'s midpoint) and its `STIsSimple` (the OGC curve rule, where two figures may meet only at a boundary point of both — so a point-touching MultiPolygon is valid and not simple), plus `geography`'s `EnvelopeCenter` / `EnvelopeAngle`, the normalized **sum of the instance's points as unit vectors** and the greatest angle to any of them, reported as 180 once that reaches 90°.
  A spatial **column** carries the property form (`SELECT Location.Lat FROM t`), decided against the query scope — the property reading when the qualifier is a spatial column, **Msg 326** when an `alias.column` reading binds too.
  **`STIsValid` answers for both**, planar and round-earth, and the round-earth rules differ: **ring orientation is load-bearing** (a hole wound with its shell is invalid), an arc that leaves the one before it isn't retracing (`LINESTRING(0 0, 2 2, 1 1)` is invalid as `geometry` and valid as `geography`), and a ring revisiting one of its own vertices **splits into lobes** that the ordinary ring rules then judge — a nested lobe wound the other way is a hole meeting its shell, which real accepts on genuine coastline data.
  An **antipodal edge** is refused while the instance is *constructed*: **Msg 24206**, under real's own `GLArgumentException`, at an angular tolerance of 1e-8 radians.
  The **Msg 24144** gate an invalid instance puts on most of the instance surface asks in the terms of the receiver's own type (`STAsText` / the ordinate reads / `STLength` / `STIsRing` / `STSrid` / `MinDbCompatibilityLevel` / `MakeValid` / `STIsValid` tolerate it; everything structural or topological raises).
  The constructive operations parse but don't evaluate yet → [`spatial.md`](docs/claude/spatial.md).
- **`ALTER DATABASE SET <option>` accept-list + database-level `COLLATE` clause**, including the two cross-database widening flags (`TRUSTWORTHY` / `DB_CHAINING`, their `sys.databases` projection, real's shipped system-database defaults, and the Msg 15309 / 5600 refusals on the databases real pins), and the **`READ_ONLY` / `READ_WRITE` access mode** — `sys.databases.is_read_only` + `DATABASEPROPERTYEX`'s `Updateability`, **Msg 3906** on every write to a read-only database (naming the *target*, so a three-part write reports the database it would have written), checked where the write happens so real's laziness follows — an `UPDATE` / `DELETE` matching no row and an `INSERT … SELECT` producing none complete quietly while `INSERT … VALUES`, `TRUNCATE` and DDL raise — with `#temp` / `##global` / table-variable writes unaffected, `COMPATIBILITY_LEVEL` the one SET option a read-only database itself refuses, and **Msg 5058** at real's own states (5 / 4) for the `master` / `tempdb` pin → [`database-options.md`](docs/claude/database-options.md).
- **Per-column / per-expression collation, coercibility precedence, and the cross-collation error family — Msg 468 / 457 where the conflict arises, and, where SQL Server's *No collation* label lets it propagate outward instead, Msg 451 naming the clause and 1-based ordinal of the output slot that couldn't settle it, Msg 4191 naming the consuming operation, Msg 446 for DISTINCT / CONVERT / COLLATE, Msg 456 at an assignment target and Msg 5335 at a deduping set operator (all bound at **compile time**, so an empty rowset and a module body at CREATE raise what real raises), recognized catalog, `#temp` inheritance, name regimes (fullwidth/decomposed fold, hash canonicalization, variable names), and the per-collation ANSI storage code page** (`varchar`/`char` stored in the collation's own page — SBCS, DBCS and UTF-8 — the byte-counted `varchar(N)` budget, and Msg 459 for the Unicode-only collations), plus the **`LIKE` / `PATINDEX` matcher**, which reads the resolved collation's every half (an `_AI` name folds diacritics for `=`, DISTINCT, ORDER BY and the match alike) over a subject read as **characters rather than UTF-16 units** — a base carries its combining marks, and a surrogate pair matches one `_`, two, or none at all depending on the collation's vintage — with a class range ordered by the collation (`[a-c]` under `_CS_AS` holds `A` and `B`, not `C`), wildcards and the `ESCAPE` character matched by code point, and trailing-space slack granted to the non-Unicode operand pair alone, plus the **character-matching string scalars** — `CHARINDEX`, `REPLACE`, `TRANSLATE`, `STRING_SPLIT`'s separator and the `TRIM` family's character set — searching through that same primitive, so every half folds for them too, a match consumes *what the subject gave up* rather than the needle's length (which is why `REPLACE` over a decomposed `café` comes back four characters and a `STRING_SPLIT` on the same pair doesn't), a weightless needle is absent, and the legacy one-argument `TRIM` forms stay a plain U+0020 strip → [`collations.md`](docs/claude/collations.md).
- **Grammar-level rules** — new top-level statement parsers, dispatch-loop separators, double-quoted identifiers / QUOTED_IDENTIFIER (including the **per-object creation-time capture**, which `ANSI_NULLS` shares as metadata: a module body parses under the `QUOTED_IDENTIFIER` in effect at its CREATE rather than the caller's session, carried by swapping the session flag for the body's duration so `@@OPTIONS` / `SESSIONPROPERTY` / dynamic SQL / the plan-cache key all follow, and surfaced in `sys.sql_modules.uses_quoted_identifier` / `OBJECTPROPERTY`'s `IsQuotedIdentOn` + `ExecIsQuotedIdentOn`; plus the **Msg 1934 / 1935 SET-option gates** that refuse a write, a computed-column / filtered / XML / spatial index, a PERSISTED computed column, a `WITH (NOEXPAND)` read of an indexed view, or an XML data-type method from a session holding any of the gate's six options the wrong way — `QUOTED_IDENTIFIER` / `ANSI_NULLS` / `CONCAT_NULL_YIELDS_NULL` / `ANSI_WARNINGS` / `ANSI_PADDING` ON and `NUMERIC_ROUNDABORT` OFF, named comma-separated in real's own fixed order, with `QUOTED_IDENTIFIER` reported alone when it is the offender), unquoted identifier body characters, reserved-keyword gating (including the **compatibility-level-170 `REGEXP_LIKE` reservation**, the one word the tokenizer reserves from a given level), and the trailing-token tightenings (which a clause's own binding error yields to — `GROUP BY 'a' 'b'` is Msg 102 at the stray token where `GROUP BY 'a'` alone is Msg 164, real parsing a batch before binding any of it), plus the **end-of-batch token naming** (a syntax error names the last token consumed rather than an empty slot, Msg 102 and Msg 4145 alike, and an argument list or parenthesized expression the batch never closed is refused rather than treated as closed), plus the **module batch-position pair** — `CREATE` / `ALTER` of a VIEW / FUNCTION / TRIGGER / PROCEDURE / SCHEMA must open its batch (**Msg 111**, per-kind state), and a VIEW or FUNCTION must be its batch's *only* statement since its body runs to end-of-batch (**Msg 156** naming the keyword as the source spelled it, **Msg 102** otherwise, attributed to the module and leaving it uncreated), while a procedure or trigger swallows the trailing statement into its body → [`grammar.md`](docs/claude/grammar.md) + [`programmable.md`](docs/claude/programmable.md) + [`control-flow.md`](docs/claude/control-flow.md).
- **sqllogictest as a differential oracle** — why a live server is the oracle and the corpus's own 2008-era expected results are not, why both delta directions are diffed, the methodology traps that fabricate or inflate findings in *any* differential harness (a swallowed exception reporting as a clean run, a capped log read as a count, state divergence needing to taint its script, order-insensitive records compared as sets, comparing more than values), and the capture/replay split that removes the live server once deviance is zero.
  Corpus and harness are both local-only and unversioned → [`sqllogictest.md`](docs/claude/sqllogictest.md).
- **BACPAC import** (`Simulation.ImportBacpac` — multi-database via repeated calls, `BacpacImportOptions`, `ModelXmlReader` dispatcher, BCP wire format, `BacpacBuilder` test harness), the **phase order a module body's own bind imposes** (full-text indexes land ahead of the procedures whose `CONTAINS` binds at CREATE, retried at their old late phase when the KEY INDEX is a standalone unique index), the **identity counters advancing past the loaded rows** (real's import leaves `IDENT_CURRENT` at the data's maximum, so without it the first insert into an imported table re-issues a key the data holds), and the **model-reading conventions the loader depends on**: a bracketed name splits on separator dots only (an identifier may carry one, and Entity Framework's `[dbo].[PK_dbo.__MigrationHistory]` is what a naive last-dot split turns into a syntax error), `IsNullable` is written only when it differs from the element kind's own default and the table and table-type kinds default *opposite* ways, and constraint trust state plus an inline constraint's server-generated name ride `<Annotation>` elements rather than `<Property>` → [`bacpac-loader.md`](docs/claude/bacpac-loader.md).
- **Linked servers** (`Simulation.AddRemoteSimulation`, `sp_addlinkedserver` / `sp_dropserver`, four-part FROM routing through the remote's ADO.NET pipeline, `OPENQUERY(server,'query')` ad-hoc pass-through + compile-time schema discovery, `sys.servers`) → [`linked-servers.md`](docs/claude/linked-servers.md).
- **TDS network endpoint** — `Simulation.ListenLocalAsync` (loopback) / `ListenNetworkAsync` (all interfaces) → `SimulatedNetworkListener`, serving real SqlClient over TCP+TLS.
  Covers the SQLBatch / RPC / TM / BulkLoad message families, TVP and legacy-LOB RPC params, the `sp_cursor*` API-server-cursor family, mid-stream attention (cancel / `CommandTimeout`), MARS, TDS 8.0 `Encrypt=Strict`, and login-credential enforcement.
  Also the **projection-nullability inference** behind COLMETADATA's `fNullable` (and `SELECT … INTO`'s destination declaration, which real derives identically): the structural rules, the per-built-in table, the operator split between arithmetic and concatenation (with the **approximate-arithmetic exception** — a `float` / `real` result is NOT NULL when both operands are and each reaches the result type losing nothing, so `<float> * <int>` is NOT NULL while `<real> * <int>` and `<float> * <decimal(9, 2)>` are not), the CASE family's constant fold, and the **arm-conversion rule** — a surviving CASE-family arm or `GREATEST` / `LEAST` argument reads nullable when the arm unification's conversion could alter the value, which is what makes `COALESCE(<decimal(9, 2) col>, 0)` nullable and `COALESCE(<decimal(9, 2) col>, 0.0)` NOT NULL.
  Oracles = `*.Tests.SqlClient` + `*.Tests.Smo` → [`tds-endpoint.md`](docs/claude/tds-endpoint.md).

## Not modeled yet

Status, not decision.
Everything here is unbuilt for cost reasons and is fair game to pick up — [`backlog.md`](docs/claude/backlog.md) carries the weighting and the prospective view of the same ground.
Entries that raise a *real* SQL Server error deliberately are **not** here; they're coverage, under [Faithful rejections](#faithful-rejections).

- **Key-range coverage past a sargable predicate on a leading key prefix** — a SERIALIZABLE / HOLDLOCK read fences a key range when its predicate bounds a **leading prefix** of some key or index; a whole-table scan, a predicate on an unindexed or non-leading column, an `ORDER BY`-eliminated ordered scan and a view / multi-source read all keep the whole-table S, which is what real degenerates to for the unindexed cases.
  A SERIALIZABLE **writer** takes no fence of its own (real converts its key locks to `RangeX-X`), and two readers' ranges meet only on an *identical* interval, since ranges intern per interval and containment is tested on the write path — see [`locking.md`](docs/claude/locking.md#key-range-locks).
- **An aggregate reading only an enclosing query's columns** (`(SELECT MAX(t.col) FROM u)` inside a query over `t`) → `NotSupportedException` where the scope offers nowhere to move it to; real binds it to the outer query, collapsing that query to one row.
  Correlation itself ships, as does the rehoming where a collector exists, and a name that resolves in *no* scope is real's own Msg 207 — see [`query.md`](docs/claude/query.md#outer-scope-correlation-in-the-select-list).
- `BEGIN DISTRIBUTED TRANSACTION` → `NotSupportedException` at dispatch.
- **Cross-server DML** (`INSERT`/`UPDATE`/`DELETE`/`MERGE` through a 4-part linked-server name) → `NotSupportedException` via `BatchContext.RejectCrossServerMutation`; open a connection on the target `Simulation` instead.
  Four-part *reads* (SELECT/JOIN) ship; catalog-view reads via four-part names (`srv.db.sys.tables`) fall to Msg 208.
  Cross-*database* DML inside one `Simulation` ships, permission resolution, catalog-view metadata visibility and snapshot stamps included; the surface still reading the session's database is the `OBJECT_*` scalars' visibility gate.
  See [`schemas.md`](docs/claude/schemas.md#cross-database-writes), [`linked-servers.md`](docs/claude/linked-servers.md).
- **`SET <option>` accept-list** (`Simulation.Set.cs`): the ANSI/session-state toggles, `STATISTICS {IO|TIME|XML|PROFILE}`, `LANGUAGE` / `DATEFORMAT` / `DEADLOCK_PRIORITY` / `QUERY_GOVERNOR_COST_LIMIT` and the rest of the value-taking family — parse-and-discard.
  Unknown SET → Msg 195.
  `SET @v`, `IDENTITY_INSERT`, `NOCOUNT`, `LOCK_TIMEOUT`, `TEXTSIZE` (client-boundary LOB truncation — see [`scalars.md`](docs/claude/scalars.md)), `TRANSACTION ISOLATION LEVEL`, `QUOTED_IDENTIFIER` (and `ANSI_DEFAULTS`'s QI component), `XACT_ABORT`, `ROWCOUNT` and `DATEFIRST` carry semantic effect.
  `SET LANGUAGE` not moving `@@DATEFIRST` the way real does is the one coupling those three leave open — see [`scalars.md`](docs/claude/scalars.md#set-datefirst-and-the-parts-that-read-it).
- **`ALTER DATABASE … SET` / `COLLATE`** — see [`database-options.md`](docs/claude/database-options.md).
  Most options parse-and-discard; `COMPATIBILITY_LEVEL`, `ALLOW_SNAPSHOT_ISOLATION`, `READ_COMMITTED_SNAPSHOT`, `RECURSIVE_TRIGGERS`, `TRUSTWORTHY`, `DB_CHAINING`, `READ_ONLY` / `READ_WRITE` are load-bearing, and every option lands on the **named** database (`CURRENT` for the session's; an unhosted name is Msg 5011).
  The read-only gate covers the DML row writes and the object-DDL statements; the `GRANT` family, `sp_rename`, `sp_addextendedproperty`, `ALTER SCHEMA TRANSFER`, index `ALTER` / `DROP` and the principal / assembly DDL don't carry it yet, and the Msg 3906 state is 1 everywhere where real varies it.
- **`MERGE … OUTPUT` through a view** → `NotSupportedException` (view-column projection through `INSERTED`/`DELETED` deferred); the other MERGE shapes ship — see [`dml.md`](docs/claude/dml.md).
- Heap allocation tracking (flat page list, no IAM/PFS).
- **`ALTER AUTHORIZATION`** in every form — there is no parser for the statement, so a schema's owner is settled once by `CREATE SCHEMA … AUTHORIZATION` and an object's follows its schema.
  See [`schemas.md`](docs/claude/schemas.md#create-schemas-owner-and-its-element-list).
- T-SQL `GOTO` / labels — `IF` / `WHILE` / `BREAK` / `CONTINUE` / `RETURN` ship; unconditional jumps don't.
- **Programmable-object top-level gaps**: CLR procedures / TVFs / aggregates / UDTs (CLR *scalar functions* ship — see [`clr-assemblies.md`](docs/claude/clr-assemblies.md)), logon triggers, INSTEAD OF UPDATE/DELETE on non-updatable views, DML through a JOIN view whose own source is another JOIN view, and MERGE into a JOIN view (the single-base INSERT / UPDATE, and a chain of single-source views over such a body, ship — see [`programmable.md`](docs/claude/programmable.md#dml-through-a-join-view)), OUTPUT through views, multi-source alias-form UPDATE/DELETE through views (Msg 4405).
  Natively-compiled + CLR procedures ship at parser-fidelity only (ATOMIC boundary → session isolation; CLR proc bodies parse but `EXEC` no-ops).
  `WITH RESULT SETS`'s `AS OBJECT` / `AS TYPE` / `AS FOR XML` definition shorthands raise `NotSupportedException`, and the clause isn't accepted on a *system* procedure; its three main forms ship.
  A module body binds at CREATE and its shape is checked there, but a statement contributes at most one binder error to the report (real names every bad column reference of one statement), and a bind abandoned at a deferral reports only what it gathered and leaves the last-statement rule unrun.
  See [`programmable.md`](docs/claude/programmable.md).
- **`ALTER TABLE` shapes not built**: `SWITCH PARTITION`, and the `ALTER COLUMN … ADD | DROP {PERSISTED | MASKED}` sub-clauses (the `ROWGUIDCOL` / `SPARSE` pair ships, as do `ADD` / `DROP PERIOD FOR SYSTEM_TIME`).
  Modeled shapes in [`alter-table.md`](docs/claude/alter-table.md).
- **`hierarchyid` OrdPath tiers outside ordinals −4168..5199** — the wider 6-byte tiers aren't in the encoder/decoder table, so `Parse`/`ToString` of one raises `NotSupportedException`; storage/BACPAC round-trip those bytes opaquely.
  The modeled tier domain and everything else that ships are in [`hierarchyid.md`](docs/claude/hierarchyid.md) / [`spatial.md`](docs/claude/spatial.md).
- **`FORCESEEK`'s plan-infeasibility refusal** (Msg 8622) — real raises it whenever the planner can't honour the directive, which is every `FORCESEEK` over a query with no sargable predicate; the simulator validates the hint's index and seek columns (Msg 308 / 362 / 365) and then reads normally, having no plan to declare infeasible.
  Surface in [`query-hints.md`](docs/claude/query-hints.md).

## Quirks (modeled, not byte-identical to SQL Server)

Cross-cutting divergences with no single feature-doc home; feature-specific quirks live in their `docs/claude/` deep-dive's divergence section (via [Feature reference](#feature-reference)).

- `decimal` / `numeric`: carried in `Storage/Decimal38`, a purpose-built `UInt128` magnitude + sign + scale covering `numeric`'s whole 38-digit domain, so every value real represents computes, stores and renders here — including the declared scale's own trailing zeros (`CAST(1 AS numeric(38, 30))` carries all thirty).
  The one narrowing is the **client** edge, and it is real SqlClient's: `GetDecimal` / `GetValue` shed trailing fractional zeros to fit a .NET `decimal` and raise `OverflowException("Conversion overflows.")` when nothing can be shed.
  SqlClient answers a minority of non-fitting values with `SqlTypeException("Invalid numeric precision/scale.")` instead, off a value-dependent internal path; the simulator raises `OverflowException` uniformly — see [`arithmetic.md`](docs/claude/arithmetic.md#the-backing-type) and [`data-reader.md`](docs/claude/data-reader.md).
- `float` text formatting: .NET `G15`/`G7`, not SQL Server's `1e+015`-style scientific.
- Auto-generated constraint names: PK/UNIQUE shape `PK__<table8>__<16hex>` / `UQ__<table8>__<16hex>` (16-hex 64-bit FNV-1a); CK/FK/DF shape `CK__<table8>__[<col8>__]<8hex>` (8-hex 32-bit FNV-1a).
  Deterministic across runs, distinct from SQL Server's object-id-derived hex (won't byte-match).
- Client-surface streaming and heap-page divergences live in [`data-reader.md`](docs/claude/data-reader.md) and [`heap-storage.md`](docs/claude/heap-storage.md).
